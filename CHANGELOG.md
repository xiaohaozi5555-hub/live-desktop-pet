# Changelog / 能力状态

按能力记录当前状态、已验证方式、以及运行时还需要什么。历史演进过程见 Git 提交历史。

## 🔴 方向变更（2026-07-26）：放弃抖音官方上架，改做主播自用程序

**决策**：产品定位不变（直播互动陪伴桌宠），但**不再走抖音开放平台的上架路线**。改为做成独立程序，主播自己在直播时运行、通过直播伴侣进画面。

**为什么放弃**（当天登录开放平台后台 + 通读官方文档查证，避免以后有人再走一遍）：

1. **直播玩法的品类体系装不下我们。** 品类是 2 大类 + 11 细分形态：经典类（对战、角力）、创新类（社交、攻防、竞速、养成、比赛、条图、闯关、音舞、乱斗、整蛊）。**"创新品类"不是一个可选项**，只是那 10 个形态的统称，提案必须落进某个具体形态。而这些定义无一例外要求"胜利目标 / 胜负 / 成长目标 / 竞技"——桌宠是无胜负、无局、无结算的持续陪伴角色，一个都装不进去。最接近的是「养成」（"培育特定角色，以使其获得**数值成长**为目标"），但那要求给桌宠加成长系统，等于改产品定位，已否决。官方提案模板通篇问"玩家视角/多主播同屏对战/玩法结算/能量条"，也印证整套体系是围绕"有胜负的局"设计的。（品类规则页更新时间 2026-03-10）

2. **形态真正匹配的是「互动工具」，但该路线暂不准入。** 互动工具定位原文是"以主播为展示主体，提供丰富直播间互动效果的**助播工具**"，已有类别为投票比拼/计数计值/内容互动/计时工具——桌宠属于这个家族。但三处都挂着停止公告：文档站《互动工具接入指南》页首、飞书提案模板标题（《XX》互动工具提案**-暂不准入**）、模板正文（"插件业务目前正在进行业务升级中，暂时不进行新插件需求评审和介入"），模板最后修改 4 月 7 日，且流程里注明"暂时收敛加群方式"——连提交提案用的话题群都不放人进了。无时间表。

3. **即使互动工具开放，还有一条硬约束**：其准入黑名单第 5 条"所有能力不连接网络，不经过开发者服务器"。我们的 `services/dialogue`（DeepSeek）和 TTS（edge-tts）都是联网调用，走那条路要全部改成本地。

**顺带查实的两条，对自用版本仍然适用**：
- 直播玩法准入规则「内容安全」第 1 条：外显信息不得出现"玩家""游戏"等词。**demo 视频背景用的《深巷》菜单上有"新游戏/退出游戏"字样**，之前误判为低风险，实际属于"外显信息"。自用版不受平台约束，但如果以后再对外提交要记得处理。
- 「礼物」在两条路线下完全不是一回事：直播玩法用的是**玩法专属礼物池**（仙女棒 1 抖币 … 稀有宝箱 3000 抖币，官方提案模板的指令表就预填这几款），而互动工具的送礼数据覆盖 40+ 款常规打赏礼物（伴侣 8.0.0 起，见更新日志 2024.06.28）。桌宠要的是后者。

### 实时弹幕数据源：已选定路线（2026-07-27 调研完成）

**结论：走 DouyinBarrageGrab（系统代理解密直播伴侣自身的 WSS 流量）。** 三条候选路线都查证过，以下都是一手证据，不是转述。

**候选一：成为直播伴侣的互动工具插件。** 技术上完全摸清了——本机 `%APPDATA%\webcast_mate\interact-plugins\install\<appid>\<版本>\` 下有 7 个已装插件，直播伴侣按 `<插件>.exe --pipeName=<appid> --maxChannels=6 --mateVersion=… --layoutMode=…` 拉起，插件连 `\\.\pipe\ipc.<appid>` 命名管道，JSON 协议（`{"type":"request"…}` / `{"type":"event","eventName":"OPEN_LIVE_DATA"…}`），官方 SDK 是插件包里的 `douyinsdk.node` + `PipeSDK.dll`。可调用 `subscribeLiveInteractPluginMessage` / `getOpenRoomData(gift_pool)` / `getLiveRoomInfo` / `createLiveCard` 等。**拿到的礼物池正是 43 款常规打赏礼物**（抖音/小心心/玫瑰…嘉年华），印证了上面「礼物在两条路线下不是一回事」那条。**卡在准入**：插件包由服务端下发，要出现在插件面板里必须走互动工具准入，而那条路暂不准入。事件只有 5 类（`live_comment/live_gift/live_like/live_follow/live_fansclub`），**没有进场**。

**候选二：网页版协议逆向**（[dycast](https://github.com/skmcj/dycast) 最近提交 2026-04-12、[DouyinLiveWebFetcher](https://github.com/saermart/DouyinLiveWebFetcher)）。最轻量，但要算 `signature`/`a_bogus`，那是抖音持续对抗的点，社区 issue 里反复出现"彻底失效"。每天开播都要用的东西不能建在这上面，**否决**。

**候选三（已选）：[DouyinBarrageGrab](https://github.com/ape-byte/DouyinBarrageGrab)**，1.6k star，最近提交 2026-04-09（内容正是「直播伴侣开播/下播事件支持」）。它做系统代理 + 自签根证书，解密直播伴侣**自己已经在收**的那条 WSS 流，因此完全不需要破解签名。对外提供 WebSocket（默认 8888）推 JSON。**消息 9 类 + 2 个直播伴侣事件**，比插件通道多出**进场**和**开播/下播**，正好补上原设计的两个缺口。

**代价与已落地的对策**（读源码查实，不是推测）：

| 风险 | 源码事实 | 对策 |
|---|---|---|
| 装机器级根证书 | `TrustRootCertificate(true)`，`CertificateValidDays = 365*10`（十年） | `services/env-guard/` 快照-还原，随桌宠自动起停 |
| 它自己不会卸载 | 代码里没有任何 `RemoveTrustedRootCertificate`，`Dispose()` 只停代理 | 同上；且 arm 时自愈补做上次遗漏的清理 |
| 私钥是否共用 | 仓库未提交任何 `.pfx`；用 Titanium.Web.Proxy，`RootCertificate==null` 时 `CreateRootCertificate()` 运行时生成 | 每台机器唯一，风险收窄为"别让 `rootCert.pfx` 外流" |
| 改系统代理可能残留 | 靠改注册表工作 | env-guard 还原；**只在当前代理确实指向 `:8827` 时才还原**，否则不动（本机常挂 Shadowsocks `localhost:1080`，无脑还原会导致下播后断网） |
| 抓到别人直播间 | `processFilter` 默认含 `douyin,chrome,msedge…` | 配置收敛到只留 `直播伴侣`；「验证员」按 RoomId 统计出现过几个直播间，用真实数据复核 |

**已完成的接入（全部离线验证通过）**：
- `normalize.py:normalize_grab()`——`{Type,ProcessName,Data}` → `perception.danmaku.*`，其中 `101/102` → `command.stream_start/stream_end`（开播下播从此自动，不用再点面板按钮，见 `packages/contract/events.md`）。统计/粉丝团/分享三类**先丢弃不误报**。
- `services/perception-danmaku/wsclient.py`——手写 RFC6455 客户端，纯标准库（不给无人值守的录制加 pip 依赖）。
- `services/perception-danmaku/record_grab.py`——**「验证员」**：开播时无人值守录原始包 + 自动出体检报告。存在理由是接真流量那次主播不会开着 Claude，没人能在旁边观察。产物在 `.cache/grab/`（含观众真实昵称，**不进版本库**）。
- `services/env-guard/guard.py`——证书/代理的快照与还原。
- 验证：`verify_danmaku.py` 17/17、`verify_grab_recorder.py` 17/17、`verify_env_guard.py` 25/25，集成 5/5、跨运行时 PASS。
- 桌宠已接线：`PET_GRAB_RECORD=1` 时随主程序自动 arm/录制/cleanup；**默认关**，因为抓包程序尚未安装。

**2026-07-29 空跑实测（未开播，只验证"装得上/连得通/删得掉"）——全过，另有两处文档与实现不符**：

- ✅ 证书确认是**本机运行时生成**：`rootCert.pfx` 在启动那一刻出现在 exe 同目录，环境守护精准识别出新增 1 张 `CN=Titanium Root Certificate Authority`（指纹 `3FC4882F…EE16`），"需人工确认 0 张"。
- ✅ 8888 通道握手成功；未开播时无数据，符合预期。
- ✅ 清理验证：证书被干净删除（575 张基线复原）、系统代理还原、境外站点连通性恢复。
- ⚠️ **`listenAny` 配置项在 v2.8.0 里是死的**。源码 `WsBarrageServer.cs` 写死 `new WebSocketServer($"ws://0.0.0.0:{port}")`，没有任何分支读这个配置。所以 **8888（数据）和 8827（代理）都对局域网开放**——后者相当于一个开放的 HTTP 代理。要堵只能靠防火墙入站规则，而"直播时关防火墙"会让这条失效，两件事必须一起考虑。
- ⚠️ **它退出时不是"还原"代理，而是"抹平"**。实测 Watchdog 把 `ProxyEnable` 置 0、`ProxyServer` 清空，**连本机原有的 Shadowsocks 设置一起干掉**。env-guard 原先只认"当前指向 :8827 才还原"，因此漏掉这种情况——已修：基线开着代理而当前被抹平时也还原，且**写回前先探测目标端口是否存活**（还原到一个死掉的代理 = 整机断网，现象跟宽带故障一模一样）。
- 📌 `upstreamProxy=127.0.0.1:1080` 实测有效（经它的代理访问境外站点 204、直连超时），顺带证明配置文件确实被读取，即 `processFilter=直播伴侣` 也生效，浏览器/抖音客户端流量不经手。
  - ✅ **订正（2026-08-11，主播拍板）：这一项不用配，保持空着。** 原文写的是"**本机必须配这一项**，否则接管系统代理会切断依赖 SS 的连接"——**这个结论是错的**，它把"开发时"的环境当成了"开播时"的环境。核查发现实际配置里 `upstreamProxy=""` 一直是空的（08-03 的备份里也是空的，不是后来被改坏的），而主播确认**开播时 Shadowsocks 本来就是关着的**，压根没有"依赖 SS 的连接"需要保住。这跟另一条独立证据吻合：07-30 做卡关攻略联网搜索时特意**强制直连不走系统代理**，理由正是"本机 SS 出口在美国、开播时 VPN 关着，走代理会变成开发时好用、开播时失效"。
  - 📌 **教训**：这是一条**只在开发环境成立、却被写成永久约束**的结论。开发时中控台自己要挂 SS 才能联网，于是把"我需要 SS 活着"误当成了"系统需要 SS 活着"。写环境类约束时要分清"谁在什么时候需要它"——开播态和开发态是两套环境。

**观众分级：自动判定已实现，但默认关闭（2026-07-29 当天定，当天又关）**：`services/perception-danmaku/autotier.py` 能依据抓包数据里的 `fansclub_level` / `is_follow_anchor` 自动发 `command.set_viewer_tier`（带 `source:"auto"`）。**但它默认不启用**——用户当天指出，抖音的「会员」和「星守护」是**各自独立的付费身份，跟粉丝团几级没有关系**，最初拿粉丝团等级去近似的映射本来就是错的。按错映射自动打标会把错误档位写进 dialogue 的观众画像库、进而影响真实回复策略，比"没有自动分级"更糟。所以现在**不猜**：等真实开播由「验证员」记下每位观众身上出现过的全部身份字段，看清楚会员/守护到底在哪个字段，再回来把 `tier_of()` 改对。在那之前分级只走主播手动打标（面板照旧可用）。

- 打开方式是 opt-in：`PET_AUTO_TIER=1` 或 `run.py --auto-tier`（**注意：早期文档里的 `--no-auto-tier` 已不存在**，开关方向后来反了过来）。阈值 `PET_TIER_MEMBER_LEVEL`(默认1) / `PET_TIER_STAR_LEVEL`(默认15) 仅在显式打开时才生效。
- **手动始终优先**（这条规则本身是对的，保留）：`memory.set_tier()` 遇到 `source='auto'` 且该观众已被主播手动打过标就跳过不覆盖（主播掌握弹幕之外的信息，机器不该拿一个等级数字推翻他）；面板手动打标完整保留，既是兜底也是最终裁决。

**接真流量时要用验证员回答的问题**（现在都是兜底实现）：礼物数量看 `GiftCount` 还是 `RepeatCount`；`DiamondCount` 单位是否抖币（插件通道实测是"分"，差 10 倍）；粉丝团 `Type` 怎么区分新加入/升级；消息到底带不带时间戳；实际会出现几个 RoomId。

**改动面确实很小**：契约、总线、brain 决策、character 渲染全都与数据来源无关，`normalize.py` 本就是"来源适配器 + 统一归一化"结构，这次只加了一个适配器，其余各层一行没动。

**已完成、可继续用的**：两条 demo 视频（`桌面/抖音提案demo视频/`）虽然不再用于提案，但作为功能演示和自查素材仍然有效。

## 桌宠窗口的直播采集（2026-07-30 解决）

**长期卡点**：直播伴侣用窗口/进程采集桌宠时画面定格，只有全屏/截屏采集正常。查了好几轮都没治好，原因是**根因有两层，每次只碰了一层**。

**可用配置（实测走通）**：桌宠「不透明窗 + 纯绿底 `#00FF00` + 关硬件加速」，直播伴侣用「窗口捕获」源并对该源开「绿幕抠图」。`开播模式.cmd` 已默认这么起（`PET_OPAQUE=1` + `--no-gpu`），控制台「直播采集」有切换按钮。

| 组合 | 结果 |
|---|---|
| 透明窗 + 窗口捕获 | 定格——透明窗是 Windows 分层窗口，逐窗口采集拿不到新帧 |
| 不透明 + GPU 合成 + 窗口捕获 | 一片纯白——Chromium 画面由 GPU 进程直接交给系统合成器，窗口在 GDI 意义上是空的 |
| 不透明 + 软件渲染 + 窗口捕获 | ✅ 成功 |
| 透明窗 + 截屏捕获 | 能采到动态画面，但该源类型**没有**绿幕抠图，背景去不掉 |

**枢纽事实**（用户 2026-07-30 在软件里实地确认）：「窗口捕获」/「捕捉进程」/「捕捉游戏」**有**绿幕抠图，「截屏捕获」**没有**。⚠️ 中控台此前据官方文档判断成"抠图只绑摄像头"，**是错的**——那页叫「绿幕人像抠图」，看着像摄像头专属，实际窗口源也有。**涉及第三方软件的具体功能位置，用户实地看到的优先于文档推断。**

**已排除、别再走的两个方向**：①「允许窗口透明」（抖音官方文档做法）用户实测无效；② 模拟成虚拟摄像头——可行但用户明确暂不考虑，且现在不需要。

**已知质量风险**：立绘边缘是半透明抗锯齿像素，压绿底后混进绿色，抠完可能留绿边。先调抠图相似度/羽化，不行则给角色加描边（美术决定）。换色用 `PET_CHROMA`；默认绿是量过的（108 张立绘离纯绿色距 196，品红 187、纯蓝 171，绿最安全）。

**运行时改不了，所以切换=重启**：`transparent` 是 BrowserWindow 构造参数、硬件加速开关必须在 app ready 前调用。控制台按钮做的是"带新参数 `app.relaunch`"，且**开播中（`PET_GRAB=1`）禁用**——重启会走 envGuard 的 cleanup→arm，等于直播途中删了重装根证书、拆了重设系统代理。另需注意 `--transparent` 这个反向开关不能省：重启继承 `PET_OPAQUE` 环境变量，没有反向开关就切不回透明。

⚠️ 软件渲染多吃 CPU。260×340 小窗理论上负担不大，但直播时同时在跑游戏；真卡了还有更轻的中间档（只关 GPU 合成、保留 GPU 光栅化），未实现。

## 弹幕反应、回复分层、卡关联网搜索、记忆分层（2026-07-30 下午，四个提交）

**背景**：当天早些时候用 `开播模式.cmd` 真开播一次，抓包链路验证成功（908 条真实数据，来源
进程全是直播伴侣，只出现 1 个 RoomId），但**桌宠对弹幕毫无反应**，同一场还暴露了记忆缺失、
语音打扰直播等一批产品问题。以下四项都是当天基于这份真实数据做的。

### 1. 弹幕链路修通（commit `3bdc487`）

真实数据里 `Data` 是一段 **JSON 字符串，不是对象**，`normalize_grab()` 直接当 dict 用，
200 条全报 `AttributeError: 'str' object has no attribute 'get'`——现象就是"黑终端里弹幕
刷得好好的，桌宠毫无反应"。**离线夹具里 Data 写成了对象，所以离线全绿也测不出来**，已按
真实形状补回归测试（`verify_danmaku.py`）。拿录制的 908 条重放：907 条归一化成功。

顺带从真实数据确认：`DiamondCount` 单位就是抖币 1:1（跟插件通道当年"是分、差 10 倍"的
结论不同，以真实抓包为准）；**会员/星守护在这个数据源里根本没有对应字段**（`PayLevel` 是
0~40 的财富等级，不是会员标志），只能主播在控制台手动勾选；只出现 1 个 RoomId，没串台。

### 2. 回复分层 + 语音配速（commit `3bdc487`、`6fd7925`）

主播实播反馈"每条都回很累，一直有语音影响直播"。定了分层：会员/星守护/灯牌≥8级/本场
送礼>1000钻算 VIP，必回；普通观众要喊到"魔丸"才回，且限流每分钟 1 条。大部分回复从
`speak`（语音）改成 `show_bubble`（纯文字气泡）。

分层做完后用真实录制数据算：309 位观众里灯牌≥8 的只有 15 人（5%），但这 15 人发了全场
88% 的弹幕，"VIP 必回不限流"会让桌宠约每 36 秒出声一次。主播提出该按弹幕间隔而不是一刀切
限流——算出 VIP 弹幕间隔中位数其实是 66 秒（不是刷屏，是"每分钟一句"的正常节奏），所以
最终两层：同人 30 秒内连发降级成文字；全局配速 120 秒内只出一次声，没轮上的照样回复只是
不出声。同一份真实数据复算：出声 181 → 34 次（每 3 分钟一次），回复总数几乎不变。送礼
两层都绕过（金额够门槛就出声，不受配速/连发限制）。

`show_bubble` 队列此前不存在（两条纯文字回复前后脚到达会互相顶掉），已补上跟语音共用同一
显示队列，默认停留 8 秒、排队超 2 条时缩短到 5 秒；掐麦回报改成只在本轮真出过声时才发。

### 3. 卡关攻略接入真实联网搜索（commit `dfd01c9`）

之前完全靠模型内置知识瞎编——同一个问题在六种调法下给出六个互相矛盾的答案（7/4~5/12/6/7/26
块），最危险一次是给 DeepSeek 传了个它不支持的搜索参数，参数被静默忽略，但模型却因此开口
就说"根据我搜索到的信息"、编出三个真实域名链接和虚构原文引述。**结论：靠"给模型开个联网
开关"这条路整个不成立**，搜索必须自己做。

裸 HTTP 逐个实测全部不可用（百度桩页、搜狗验证码、**Bing 国内版页面能开但结果是别的查询的
缓存**——这条最阴险，光看状态码和页面长度会误判成可用）。真实浏览器 + 百度能拿到对题结果。
没引入 Playwright（C 盘紧张），复用桌宠自己的 Electron 开隐藏窗口去搜（`--search=<词>`），
**强制直连不走系统代理**（本机 SS 出口在美国，开播时 VPN 关着，走代理会变成"开发时好用、
开播时失效"，强制直连后还快了 3 倍）。

新增 `window_title.py` 自动认游戏名（持续跟踪前台窗口，因为触发攻略那一刻游戏多半已经不在
前台）；`websearch.py` 搜索 + 抓正文 + 查询词构造（游戏名要锚定成"《X》游戏"，否则搜出来的
是词典条目）；`vision.walkthrough()` 只拿搜到的真材料喂给模型总结，搜不到就如实说没搜到、
绝不给出任何来源。控制台加了「卡关求助」输入框，让主播打字补充"卡在哪"——这是搜索质量的
主要来源，游戏名能自动认，但"第几关、试过什么"只有主播知道。

### 4. 记忆分层（commit `cb755cd`、`6fd7925`）

此前桌宠连主播上一句说了什么都记不住，每次调用都是全新的。做成 session 模型：主播 session
= 一场直播，整场对话原样留在上下文（实测中文约 1.5~1.8 汉字/token，主播侧 3 小时一场才约
1.7 万 token，DeepSeek V4 窗口 100 万，装得下不用压缩不用检索）；下播把本场对话总结成
"主播是个什么样的人、怎么相处"存进 `streamer_profile` 表，开播读回来拼进 system prompt，
做成**冻结快照**（整场不再变，配合 DeepSeek 上下文缓存要求前缀完全一致）。观众记忆按
`viewers` 表已有的字段扩展成有限记忆：超过 10 场没来就忘、最多记 30 人、按场次不按天数
（主播不定期开播），member/star_guardian 手动打标的豁免淘汰。

**软隔离**：跨 session 只共享结构化事实（昵称/档位/送礼额），不共享原始弹幕文本和主播档案
——弹幕是不可信输入，档案是主播私人印象，两者都不该混进对方的上下文。

新增 `services/perception-danmaku/replay_grab.py`：把录制的真实数据按原节奏重放到总线，
不用真开播就能验证整条反应链，跟数据链路（`run.py`）和验证员（`record_grab.py`）都不是
一回事，真开播时不该跑它。

**⚠️ 这四项目前只做了离线验证（用真实录制数据重放/单元测试），还没有真开播实测过。**

## 回复分层与语音配速：离线重放精确核验（2026-08-01）

**背景**：07-30 下午那批新东西（弹幕反应/分层/配速/搜索/记忆）提交后一直没有真开播验证过。
08-01 用中控台自建的纯订阅总线观察脚本（只 subscribe 不 publish，落盘不进数据链路，跟
`record_grab.py` 同一原则）+ `replay_grab.py --speed 1 --minutes 5 --no-lifecycle` 原速
重放 07-30 录制数据的前 5 分钟（40 条真实事件），逐条手算核对分层/配速规则是否命中，并与
`startup.log` 的 `[dialogue][耗时]`/`[tts][耗时]` 交叉验证。

**结果**：23 条 dialogue 个性化回复，规则判定 **100% 匹配**（两份独立日志的条数都是 23，
互相对得上）。2 次出声、21 次降级文字，2 次出声间隔 123.4 秒（≥120s 门槛）；抓到两个精确
边界样本——114.75s（差不到 5 秒未达 120s）判文字、123.4s 判出声，证明配速不是"大致有效"，
是精确卡在阈值上生效的。同人连发降级（30s）、非 VIP 且未喊"魔丸"→正确沉默，均有真实样本
命中。掐麦门（防自问自答）8 次开关配对，无一次卡死或不对称。

**这次没测到、仍待真开播或专项测试的**：① 送礼金额够大且绕过限流出声这条分支本次窗口未
触发（一条 99 钻礼物先撞上同人 20 秒冷却，没走到那条分支），代码读过没问题但缺真实样本；
② 主播语音对话/记忆——这次只有弹幕没有语音，完全没跑到；③ 下播档案总结——用
`--no-lifecycle` 故意跳过；④ 卡关求助；⑤ 主动互动（10分钟一条，5分钟窗口内弹幕一直很密，
没出现触发它的空闲期）；⑥ 气泡"排队超2条缩到5秒"——那是渲染进程自己的显示逻辑，总线消息
里只有基础的 8000ms 参数，日志看不到实际显示时长。

**顺带**：3 次语音合成耗时 ~12s（常驻进程本身没问题），跟已知"到微软网络往返长尾出现过
12.6s"是同一类，不是新 bug，但这次窗口连续撞见 3 次，记一笔。重放会往真实
`dialogue_memory.db` 写 7 位真实观众的**本场**字段（`last_seen_ts`/`gift_total_session`），
跨场字段（`sessions_seen`/`gift_total_lifetime`）不动、不产生任何档案文本——写入用的是重放
的原始 07-30 时间戳，基本等于把历史真实值又写了一遍，无害。

## 首场真开播（2026-08-01 晚）：中途下播，两个真问题

**背景**：用「开播模式」桌面快捷方式真开播，是 07-30 那批新东西第一次接受真实观众/真实环境
检验。中途主播把桌宠关了，因为语音欢迎刷屏太吵；同时也复现了"移出视野导致采集定格"的老
问题。以下两条都是当场诊断+处理的记录。

### 1. 进场欢迎语音刷屏——已修，已离线verify，未live复测

**现象**：每个新观众进场都会 `speak`（出声）一次"欢迎 XX 进入直播间~"，冷却只有 3 秒。
真实观众源源不断进场时，桌宠几乎连续出声，主播反馈"不停在说欢迎词，影响直播"。这是
07-30 那次"分层"改造漏掉的一块——当时只处理了弹幕聊天回复（`dialogue.py`）和礼物答谢
（`brain.py` 的 `GIFT_SPEAK_MIN_COINS`），进场欢迎（同样在 `brain.py`，但是完全独立的代码
路径）没有被同一套标准检查到。

**修法**（`services/brain/brain.py`）：① 出声降级成纯文字气泡（`show_bubble`）；② 不再
来一个欢迎一个，改成"欢迎窗口"——每 `WELCOME_CYCLE_MS`(10分钟) 只开放
`WELCOME_WINDOW_MS`(1分钟)，窗口外进场的观众安静不欢迎（仍会被 `_remember` 记住昵称）。
窗口内部沿用原有的 3 秒合批逻辑，避免开窗瞬间涌进一堆人时气泡刷得比能读还快。
`verify_brain.py` 新增 6 条断言覆盖"窗口开时欢迎/窗口关时安静/下一周期恢复"，40/40 全过。

**⚠️ 这次只做了离线验证**（规则判定本身没问题），10分钟/1分钟这两个数字是照搬"每3分钟
一次语音"那次的经验直接定的，没有真实数据支撑，**下次开播要看这个节奏实际听感对不对**，
不对就是改这两个常量的事。

### 2. 移出视野导致采集定格——复现，未修复，根因还没定位

**现象**：主播点"移出视野"（`stream-mode`控制台按钮之外的另一个开关，`offscreen-set`），
直播伴侣窗口捕获画面又卡死了——跟 07-30 记录的老问题（见下方"桌宠窗口的直播采集"一节）
现象一样。当时的修法是"留 1 像素在桌面内"（`OFFSCREEN_KEEP_PX=1`，`main.js`），今晚证明
**这个修法不够，或者根本没解决真正的病因**。

**已排除的假设**（08-01 当场查证，不是推测）：
- ❌ 多屏坐标计算错误（`offscreenPoint()` 分别取所有屏幕 minX/minY，理论上在非规则多屏
  布局下可能算出一个不落在任何屏幕内的点）——**用户机器只有一块屏幕**（`2560×1440`，
  primary，bounds 原点 (0,0)），这个假设的前提根本不成立。
- ❌ DPI 缩放导致坐标换算误差——实测 `DpiX=DpiY=96`，即 100% 缩放，无缩放换算问题。

**当前最可能的方向**（未验证，留给下次查）：07-30 的修法解决的是 **Chromium 自己**判定
"完全不可见就停止画帧"（`disable-features=CalculateNativeWinOcclusion` 那组开关管的范围）。
但直播伴侣的"窗口捕获"很可能走的是 Windows 自己的合成器/捕获管线（DWM 或 Windows Graphics
Capture API），这一层是否愿意为一个"只剩 1 个物理像素在桌面内"的窗口维持一份实时画面，
**不受 Chromium 命令行开关控制**，是完全独立的另一层，07-30 的修法可能只解决了半个问题。
下次排查需要真实复现（桌宠+直播伴侣同时跑，边切边看预览画面）才能确认，纯读代码/查坐标
查不出这一层。

**当前workaround**：不用"移出视野"，桌宠留在桌面上——不影响观众通过窗口捕获看到的画面，
只是主播自己也会看见它。

## 首场真开播复盘（2026-08-01 深夜）：验证员本身带病判卷，桌宠核心逻辑是对的

**背景**：上一节两个真问题处理完后，主播又开了一场（`raw-20260801-114712.jsonl`，1486秒/
255条真实事件：163进场/48点赞/27弹幕/13礼物），中途正常下播。验证员报告显示多类"完全没
反应"，看着很吓人：礼物 0/13、弹幕 2/27。但礼物答谢、弹幕分层这些代码 07-30/07-31 都验证过
逻辑，直觉不太对，于是没有直接信报告，而是拿真实数据把 `normalize_grab()`→`Brain.handle()`
→`Dialogue.handle()` 整条链路直接跑一遍复现（LLM 调用打桩，不产生真实请求），逐类核对。

**结论：桌宠的反应逻辑是对的，坏的是验证员自己。** 三个真 bug，全部在
`services/perception-danmaku/record_grab.py`，全部已修复 + 补了回归测试
（`verify_grab_recorder.py` 17→22 通过）：

1. **`ReactionWatcher.on_bus` 不认识 `show_bubble`**——07-30 那次"分层"把大量反应从
   `speak` 改成了 `show_bubble`，但验证员的判卷代码当时没跟着改，只认 `play_motion`/`speak`
   两种，`show_bubble` 消息进来直接被当空气漏记（连"归因给谁"都不记，但会占掉归因窗口，
   `return` 提前退出）。直接复现证明：13 条真实礼物，`Brain._gift()` 逐条正确产出
   `play_motion+show_bubble`（全都是 1~9 抖币的小额，按 07-30 定的门槛本该不出声），
   但验证员把这些"文字答谢"全部漏计成"没反应"。
2. **`EXPECT["danmaku.gift"]` 还停在 07-30 分层之前的老标准**（写死 `speak: True`），
   跟第 1 条叠加，让"语音带昵称"这个指标对礼物类系统性显示 0——这是判卷标准过期，
   不是桌宠没反应。已改成 `speak: False`（真实数据里大多数礼物确实不该出声）。
3. **`Recorder._stat()` 没做"Data 是 JSON 字符串"的解析**——这正是 07-30 修过的那个坑
   （见上方"弹幕反应…"一节）的**第二个受害函数**：`normalize_grab()` 当时改好了，但
   `_stat()` 是完全独立的另一段代码，同样的 `d.get(...)` 假设 `d` 是 dict，真实数据里
   `d` 是字符串，200 条打头就全报 `AttributeError`，后面所有基于字段的统计（直播间/
   观众身份字段分布/礼物样本…）从此全是空的——报告里那些空白section不是"真的没数据"，
   是这个函数自己没跑到统计那一步。**两次同一个坑出现在两个不同函数里**，说明这类"离线
   夹具用对象、真实数据用字符串"的字段形状差异，光靠"改一处"防不住——已给 `_stat()` 单独
   补了字符串形态的回归用例，防止出现第三次。用修好的代码重新离线分析这份真实数据，
   诊断信息全部找回来了：只有 1 个直播间（没串台）、观众身份字段详尽列了一遍（依然没有
   能标出会员/星守护的字段，`FansClub` 之外没有别的候选）。

**弹幕分层同样验证：用真实 `Dialogue` 类（LLM 调用打桩，逻辑代码不变）重放这场 27 条真实
弹幕，20 条正确判定该回复（5 条出声、15 条文字），跟手工核对"灯牌≥8 的真实弹幕数"（20条）
分毫不差。真实报告只记到 2/27，缺口比礼物那个更大——因为 dialogue 的回复要等真实 LLM 调用
（当晚实测排队+生成普遍 2.8~4.6 秒），这个真实延迟远大于 brain 的近乎同步（~1ms），
给了验证员归因窗口里"抢到最近事件"这个漏洞（见下条）更大的可乘之机。

**归因算法已重做，不用再猜时序（2026-08-01 深夜，同一轮内完成）**：一开始以为要改契约
才能根治，重新读代码后发现不用——`brain.py`/`dialogue.py` 产出 action 时，`ts` 字段本来
就原样继承自触发它的那条感知事件（`_act(type_, data, ts)` 的调用点全都这么传），哪怕
dialogue 要经过后台 worker 排队 + 等几秒 LLM 生成才真正发布，那条 action 上的 `ts` 依然
是最初那条弹幕的 `ts`，不是生成完成的时刻。**这份信息契约里一直都有，只是验证员自己
没去用**，改成按 `ts` 精确匹配（`ReactionWatcher.events_by_ts`，OrderedDict 加
`MAX_TRACKED=3000` 防止长时间不下播内存无限增长）——纯验证员内部实现改动，**不碰契约、
不改 brain.py/dialogue.py 生产代码**，风险降到最低。压力测试：模拟 4 秒延迟（比 dialogue
真实生成耗时 2.8~4.6s 更极端）、中间插入无关事件干扰，归因依然精确到 13/13、零误判——
旧版同样条件下会掉到 7/13。`verify_grab_recorder.py` 22→24 通过，新增用例专门钉死"动作
晚到 + 中间插了别的事件，仍精确对回最初触发它的那条"这个此前会出错的场景。

**顺带发现一个新问题，也没有擅自动**：真实礼物数据里 `GiftCount` 与 `RepeatCount`
**不总相等**——之前认为"始终为1，谁是总数还不确定"，今晚"美味烧鸡"礼物连击时
`RepeatCount` 从 1 累加到 9、`GiftCount` 全程为 1，形态很像"连击进行中的实时计数"而不是
"这条消息里有几个"。现在 `normalize.py` 取两者较大值当 `count`，如果 `RepeatCount` 真是
连击运行时计数，会把一次 9 连击（真实礼物价值大概率是 1×9=9 抖币）错误地当成
1+2+…+9=45 个"礼物单位"，产生 9 次独立答谢、把送礼额多算 5 倍——这会实际影响
`VIP_SESSION_COINS`(>1000算VIP)、`GIFT_SPEAK_MIN_COINS`(≥10出声) 这些真实阈值的判定。
**没有轻易改**：只有这一次连击样本，"连击结束时最后一条 RepeatCount 才是权威总数"只是
推测，需要更多真实连击样本或去查 DouyinBarrageGrab 源码里 `Combo` 字段的确切语义
（这条真实数据里 `Combo` 字段值也记录了，可以去对）才能确认，改错了会让"该出声的没出声"
反过来发生。

## 语音聊天取消"免重复唤醒词"窗口（2026-08-01 深夜）

**背景**：主播复盘时主动提出顾虑——直播时不可能一直对着桌宠说话，担心"浪费桌宠的
token"。查证后发现顾虑成立，而且比预想更直接：`services/perception-voice/intents.py`
原设计是喊一次"魔丸"开一个 **18 秒对话窗口**（`ConversationWindow`），窗口内任何有效
发言（哪怕不含唤醒词）都会被转发给 dialogue 当聊天内容送 DeepSeek，**且每句都会把窗口
续满 18 秒**。主播讲话间隔常常短于 18 秒（正常语速跟观众互动），意味着喊一次"魔丸"之后，
只要话赶话不停顿超过18秒，后续所有跟观众讲的话都会被误当成"跟桌宠聊天"，白白耗掉
真实 DeepSeek 调用的 token。

**改法**：直接删除 `ConversationWindow`，**每句都要求唤醒词**，不再有隐藏的倒计时。
主播同时提出对标"豆包"——不想自己数秒数。查证后确认：**单句"说完了没"从来都不是靠固定
秒数**，是 `run.py` 的 VAD 静音判定（`SILENCE_TAIL_MS=700ms`，外加 `MAX_SEGMENT_MS=15000`
防止一直不停被无限缓冲）——这套机制本来就跟"豆包"一样是自然停顿触发，不受这次改动影响。
这次改动砍掉的只是"喊一次能续多久"这个多轮对话层面的窗口，跟单句识别是两回事。

`services/perception-voice/verify_voice.py` 34/34（新增用例钉死"紧跟着的下一句不喊唤醒词
就不再转发"）；`services/dialogue/SPEC.md` 里原先"开一个对话窗口"的设计描述已标记撤销并
说明原因；`run.py`/`diagnose.py` 两处调用点同步去掉 `window` 参数。**这项改动还没有真实
语音测试过**（属于 HANDOFF 里"从没被真实验证过的核心链路"那批之一），下次验证语音链路时
一并确认。

## 下播档案从没真正触发过的真因 + 手动下播按钮（2026-08-01 深夜）

**背景**：真开播测试 #5（下播档案总结）时，主播发现一件怪事——明明真开播又真下播了，
数据库里却没有对应的档案记录。查证后确认：`command.stream_end` 目前**只有一条触发
路径**——抓包程序捕获到直播伴侣自己发出的真实 Type=102（下播）事件，经
`normalize_grab()` 转换而来。控制台没有"结束直播"按钮，桌宠自己关闭时
（`main.js` 的 `stopAll()`）也只是直接杀掉 dialogue 进程，不会补发这个信号。

**实测证实**：今晚真实抓包数据（`raw-20260801-114712.jsonl`）里确认**没有任何一条
Type=102**——主播全程是靠关闭控制台窗口结束的（今晚为了修 bug 关过好几次），从没有
在直播伴侣里走过"点结束直播"这条路，档案总结/观众场次记录因此一次都没真的跑过。

**顺带发现一个相关的坑**：语音意图表里"下播"这个词面已经存在，但它映射到的是 brain 的
`sleep`（休息模式，只是让桌宠闭嘴），跟 dialogue 的 `stream_end`（真正触发总结+存档）
是完全不同的两条线，只是词面撞了——真喊"魔丸，下播"不会触发档案总结。

**改法**：没有采用"语音说下播就触发"的方案——主播明确说直播时开玩笑可能随口提到这两个
字，怕误触发。改成**控制台手动按钮**（`apps/character/console.html`/`console.js`
新增"手动下播"，`command.stream_end` 之外新增的唯一入口），点击有二次确认防手滑，
文案写明"中途重启修bug时别点"。跟其余按钮走同一套 `API.publish` 路径，不用改契约、
不用改 main.js/preload.js。`node --check` 语法验证通过；**因为桌宠正在跑另一项测试
（#6/#8 长重放），没有重启验证实际点击效果**，下次重启桌宠时生效，待验证。

## 开播前全量验证跑出的一个真回归（2026-08-02）

主播说today要再开播验证上一轮改动，跑全量 verify 脚本作为开播前检查，`verify_grab_end2end.py`
从 16/16 掉到 14/16（后来查明其中一条是环境噪音导致的偶发波动，重跑三次稳定在真正的
1 条失败）。**根因是自己给自己挖的坑**：08-01 那批修复里，brain.py 的进场欢迎从 `speak`
改成了 `show_bubble`（见"首场真开播复盘"一节），但 `record_grab.py` 的 `EXPECT` 表里
`danmaku.enter` 那行没有跟着改，还写着 `speak: True`——跟同一晚早些时候修的 `danmaku.gift`
是**一模一样的坑**，这次是自己在同一个晚上又踩了一遍。已改成 `speak: False`，`verify_grab_end2end.py`
里原本用"进场欢迎"测"验证员核到语音昵称"这条断言也换成了"关注答谢"（`danmaku.follow`，
分层后依然出声的那类），三次重跑稳定 16/16。**教训写进代码注释里了**：以后改 brain.py
任何一类反应的出声方式，必须同时检查 `record_grab.py` 的 `EXPECT` 表有没有跟着变。

## 弹幕中途断流：读第三方源码定位（2026-08-02）

**现象**：真开播 76 分钟，抓包只拿到 29 条，且**全部集中在前 5.2 分钟**，之后 58 分钟一条
没有。抓包程序进程还活着、我方 WebSocket 连接也没断，主播全程不知情，下播复盘才发现。

⚠️ **先纠正中控台自己的一个错误判断**：当时我说"抓包程序是闭源的所以看不到日志"——**这是
错的**，它是 GitHub 开源项目（`ape-byte/DouyinBarrageGrab`），本项目 07-27 就读过它的源码
（`listenAny` 是死的、`CertificateValidDays=365*10` 这些结论都是那时读出来的）。用"闭源"
当查不下去的理由，是没查证就下结论。主播当场指出要去逆向，才有了下面这些。

**证据链（全部一手，不是推测）**：

1. **程序自己有错误日志**，就在 `D:\BarrageGrab\logs\error\<日期>.log`——第一次排查时没去看。
   今天那份只有一条：`11:33:36 处理弹幕数据包时出错: ProtoBuf Invalid wire-type (String)`，
   抛在 `BarrageGrab.WssBarrageGrab.Proxy_OnWebSocketData`。
2. **时间完全对上**：最后一条弹幕 `11:33:34` → 2 秒后这条异常 → 之后再无数据。
3. **但这条异常不是病因**。读源码（`BarrageGrab/Server/WssBarrageGrab.cs`）确认
   `Proxy_OnWebSocketData` 整个包在 `try/catch` 里，`catch` 只是 `Logger.LogError` 然后继续——
   **单条解析失败根本不会让它停下来**。而日志里只有这一条错误、之后既无数据也无新错误，
   说明 `11:33:36` 之后这个函数**再没被调用过**，数据在更上游就断了。
4. **上游是一个硬编码正则**（`BarrageGrab/Proxy/TitaniumProxy.cs:46`）：
   ```csharp
   private readonly Regex webcastBarrageReg = new Regex(@"webcast\d+-ws-web-\w+\.(douyin|amemv)\.com");
   ```
   只有 URI 匹配它的 WebSocket 连接才会挂上 `WebSocket_DataReceived` 处理器（同文件 331-338 行）。
   **处理器只在连接建立那一刻挂一次**。

**结论（高置信度，但没有 100% 直接证据）**：`11:33:34` 直播伴侣与抖音之间那条 WSS 断了
（那个截断的包正是连接被拆到一半的特征，所以才解析失败），直播伴侣随后重连，但新连接
**没有被重新挂上处理器**——最可能是新端点不匹配上面那个 `-ws-web-` 正则（源码里另有注释
提到直播伴侣用的是 `webcast5-mate-lf.amemv.com` 这类 **mate** 域名，跟 `-ws-web-` 不是一
个形状）。缺的直接证据是重连时那条 `订阅到新的弹幕流地址` 日志——见下条，它没落盘。

**为什么没有更多线索可查**：`Logger.cs` 用 `LoadConfigurationFromAssemblyResource` 从
**内嵌资源**加载 NLog 配置，而那份配置（`Configs/nlog.config`）的规则是
`minlevel="Error" writeTo="file"` + `minlevel="Info" writeTo="console"`——**Info 级只进控制台，
不落盘**，且因为配置是编译进 exe 的，外部放一份 nlog.config 也覆盖不了。所以"有没有重新订阅"
这个决定性证据，当时打在那个黑窗口里，窗口一关就没了。

**❌ 一条看着像解药、实际没用的路（别再走）**：配置里有
`forcePolling`，注释写着"对于容易断开连接或者更加追求稳定的直播间…虽然响应速度不如
WebSocket，但是绝对稳定！"，README 也把"连接一段时间后就看不到弹幕了"列为已知问题并推荐
它。**但读实现发现它对我们无效**：`AppSetting.cs` 里这个属性的注释是
「强制启用轮询模式获取弹幕**(仅对浏览器和客户端生效)**」，实现方式是在
`/obj/static/webcast/douyin_live` 这个**直播页 JS 脚本**上做手脚（`TitaniumProxy.cs:507`）。
我们用的是**直播伴侣**这个原生客户端，根本不加载那个页面脚本。**光看配置注释会误以为
是解药，必须读实现才发现不是。**

**版本**：本机 v2.8.0，是仓库最新 release（2026-04-09），没有升级版本可用。

### 决定性发现：直播伴侣的代理配置带 `direct://` 回退（2026-08-03）

主播追问"恢复会打断直播伴侣？我会被强制下播吗？"，去读了 `Utility/LiveCompanHelper.cs`，
挖到这次调查最关键的一处事实——抓包程序接管直播伴侣的方式，是**改直播伴侣自己的启动脚本**，
往里注入（`LiveCompanHelper.cs:211`）：

```
--proxy-server = 127.0.0.1:8827,direct://
```

（顺带确认：**直播伴侣本身是个 Electron 应用**，所以才有 `commandLine.appendSwitch` 可改。）

**这一行同时回答了两个问题**：

1. **重启抓包程序不会让主播掉播。** 结尾那个 `direct://` 是 Chromium 的代理回退规则——
   代理不可用时自动走直连，网络不断；而推流本身根本不走这个 HTTP 代理。被打断的只有
   "弹幕那条连接"。（此前 CHANGELOG 里"重启会打断直播伴侣"的说法太笼统，容易被读成会掉播。）
2. **但它也给出了一个更可能的断流原因**：Chromium 发现代理有问题时会把它标记为坏代理、
   **自动改走直连**——从此抓包程序什么也看不到，而它自己还好好活着、我方 WebSocket 也没断。
   这跟 08-02 的现象完全吻合。

⚠️ **如果是这个原因，上一节那个"重启抓包程序"的自动恢复救不回来**：那时直播伴侣走的是直连，
跟抓包程序死活无关，重启它不会让直播伴侣重新走回代理。现在有两个都能解释现象的假设
（① WS 端点正则不匹配 ② Chromium 回退直连），**从已有数据分辨不了**。自动恢复保留（安全、
有次数上限，在假设①下确实有效），但**不能当成问题已解决**。

### 真正的解法：弹幕备用源（2026-08-03）

既然直播伴侣那条连接我们管不了（重启它＝打断直播），那就**自己再开一条**：桌宠自己开一个
隐藏窗口挂在主播的直播间网页上、走同一个代理，当第二条弹幕来源。

**可行的关键**：抓包程序 `DoMessage` **按 msgId 去重**（每类消息保留最近 300 条 ID），所以
两条源同时喂不会产生重复弹幕。

- 实现在 `apps/character/main.js`（`startBarrageBackup`），只在开播模式（`PET_GRAB=1`）起。
- **独立会话分区**（`persist:barrage-backup`）：它要走代理 `127.0.0.1:8827`，而卡关搜索那个
  窗口要强制直连，两者绝不能互相污染。
- `backgroundThrottling: false` **不能省**——隐藏窗口默认被降频，降频会让这条 WebSocket 收得
  断断续续，而备用源的全部意义就是"平时不吭声、关键时刻还活着"。
- 静音 + 默认拦掉视频流（`resourceType==='media'` 及 flv/m3u8/ts），只要弹幕不要画面，省 CPU
  ——直播时本来就在跑游戏 + 软件渲染。真出问题设 `PET_BARRAGE_BACKUP_VIDEO=1` 放行。
- 直播间号**自动认**：优先 `PET_LIVE_ROOM_ID`，否则从最近一次录制的原始包里正则提取
  （已用 08-02 那份真实录制实测通过，认出 `91935611423`）。
- **断流时重新加载这个窗口**（`main.js` 订阅 `danmaku.health`）——这是**零风险的那条恢复
  路径**，重开的是我们自己的窗口，完全不碰直播伴侣，不会影响推流更不会掉播。1 分钟内不重复
  重载，免得告警连发时刷个不停。

⚠️ **改了第三方配置**：`D:\BarrageGrab\WssBarrageServer.exe.config` 的 `processFilter`
从 `直播伴侣` 改成 `直播伴侣,electron`——抓包程序**只处理白名单进程的流量**
（`WssBarrageGrab.cs:108` 直接 return），我们的窗口进程名是 `electron`，不加会被整个过滤掉。
**保留了"直播伴侣"**，所以最坏情况是备用源不工作、主链路照旧。原文件已备份为
`WssBarrageServer.exe.config.bak-20260803`。⚠️ **改完要重启抓包程序才生效**（配置文件首行
就写着"配置更改后重启才能生效"）。

📌 **留着的下一个开关**：`forcePolling=true` 会让**网页端**改用 HTTP 轮询取弹幕（作者称
"绝对稳定"），它对直播伴侣无效（见上文），但**对我们这条备用源是有效的**。这次没有一起打开——
一次只动一个变量，否则出问题分不清是备用源本身还是轮询模式导致的。如果备用源的 WS 也被
证明不稳，这就是下一个该拉的杆。

### 上一版做的：自动恢复（不只是告警）

⚠️ **先纠正中控台的第二个错误判断**：写这节时说过"根因在第三方程序内部，我们改不了"——
主播当场反问"你确定程序不能改吗？不是运行在我们本地的东西吗？"。**他是对的**：那是本地
运行的开源 C# 程序，fork + 打补丁 + 重新编译完全做得到，"根因在第三方"不等于"改不了"。
当天没走这条路的真实原因**不是不能，而是代价与时机**：本机只有 .NET 运行时、没有 SDK / VS /
msbuild，而它是 .NET Framework 项目，编译需要装 VS Build Tools（数 GB，本机 C 盘常年紧张），
且不该在开播前拿一个自编译的 exe 替换掉正在用的版本。**这条路留着，不是关掉了。**

主播还纠正了目标本身："重点并不是说让我知道，而是必须要恢复。" 告警只是不再蒙在鼓里，
恢复才是要的东西。所以做成了**告警 + 自动恢复**：

**恢复机制：断流就自动重启抓包程序。** 为什么这能救回来——它靠系统代理拦截直播伴侣的流量，
而 WebSocket 的拦截处理器**只在连接建立那一刻挂一次**；断流是那条连接上的状态坏了。杀掉
抓包程序 → 它的代理没了 → 直播伴侣那条走代理的连接被迫断开 → 直播伴侣重连 → 新连接被新
实例重新拦截，状态全新。**这条推理来自源码，但还没被真开播验证过**，验证方式就是看告警之后
有没有自动跟上一条"✔ 弹幕恢复了"。

🔒 **一条安全红线，已用源码级测试钉死**：恢复流程里**绝不允许强杀（`/F`）抓包程序**。它正常
退出时会把系统代理抹平（ProxyEnable=0），网络因此回到直连、一切正常；强杀会跳过这一步，
让系统代理仍然指着一个**没人监听的 8827 端口——那等于整机断网**，现象跟宽带故障一模一样。
所以宁可这次恢复失败，也绝不强杀。`verify_danmaku.py` 里有一条断言直接扫 `_restart_grabber`
的源码，谁以后图省事加个 `/F` 就会立刻红。
重试上限 3 次（`PET_DANMAKU_MAX_RECOVER`），因为反复重启只会不停打断直播伴侣的连接；
真恢复了计数清零。整个自动恢复可用 `PET_DANMAKU_AUTO_RECOVER=0` 关掉。

📌 **不谎报成功**：重启后端口起来了只发 `recovery:'ok'`（意思是"程序活了"），**弹幕真的回来
才发 `ok:true`**。判断有没有恢复只认后者，两者别混。

其余（让主播当场知道）：

- **新增契约事件 `perception.danmaku.health`**（`{ok, silent_ms, connected}`，三处已同步：
  `events.md` / `perception.schema.json` / `validate.py`）。由 `perception-danmaku/run.py`
  发布——它是唯一知道"我多久没收到包"的人。`connected:true` 而 `ok:false` 正是这次故障的
  特征（连接好好的就是不来数据），能一眼分开"抓包程序挂了"和"活着但不出数据"。
  ⚠️ 这不违反「验证员不进数据链路」原则：报的是**自己输入断了**这个客观状态，不是自评质量。
- **控制台顶部整幅告警条**（呼吸动画，不是角落小字）+ 对话区文字说明，默认 5 分钟无弹幕
  触发、每 10 分钟重复提醒，恢复时补一条"✔ 弹幕恢复了"。阈值可用
  `PET_DANMAKU_STALL_MS` / `PET_DANMAKU_STALL_REPEAT_MS` 调。
- **修了一个让排查变瞎的真 bug**：`perception-danmaku` / `perception-game` / `brain` /
  `record_grab` 四个服务的 stdout 被 Electron 用管道接走，Python 对管道**默认全缓冲**，
  于是这些服务的日志整场卡在缓冲区里出不来——今天 `startup.log` 里 danmaku 服务一行都没有、
  连 `[game] 找攻略中…` 和搜索来源也全丢了，正是这个原因。已全部改成
  `sys.stdout.reconfigure(..., line_buffering=True)`。**下次再断流，日志里会有完整现场。**

## 卡关攻略的两处体验问题（2026-08-02 主播实测反馈）

**① 截到的不是游戏画面**：主播原话"我必须要切到桌宠控制面板点那个卡关的按钮，所以我点了
之后很有可能后面截取的并不是我的游戏画面而是其他乱七八糟的"。确实如此——点按钮那一刻前台
就是控制台，代码立刻截屏，截到的就是控制台自己。已加 5 秒延迟（`CONSOLE_SHOT_DELAY_S`，
可用 `PET_WALKTHROUGH_SHOT_DELAY_S` 调），**只对控制台按钮生效**，语音触发不等——那条路径
本来人就在游戏里。控制台按钮的提示文案同步改成"快切回游戏画面！5 秒后才截图"。

**② 结果不知道去哪看**：主播原话"攻略的反馈我并不知道怎么给我，桌宠没有告诉我，我也没有
找到哪里可以查看。我希望最后是桌宠直接语音的方式告诉我"。查日志发现**语音其实已经说了**
（`17:03:44` 有真实 TTS："卡住啦？试试：主播！鹿九对话时记得选'递…"），所以缺的不是"语音
播报"这个能力，而是**说完就没了**——`speak` 的气泡只在音频播放期间显示，播完即消失，而攻略
是要照着做的信息，主播多半正忙着操作没法立刻抬头。已改成"出声 + 再留一条 30 秒的长气泡"
（`WALKTHROUGH_BUBBLE_MS`），听完还能回头看一眼。攻略来源链接本来就在控制台对话区里，
这次一并在按钮提示里说明了。

## 🔴 抓包程序把直播伴侣改坏了："文件损坏，请重新安装"（2026-08-04）

**现象**：主播打不开直播伴侣，提示文件损坏、让重装。**是我们造成的。**

**根因（实测确认，不是推测）**：`liveCompanHookSwitch=true` 时，抓包程序**会直接改写直播伴侣
安装目录里的 `resources\app\index.js`**（`Utility/LiveCompanHelper.cs:SwitchSetup`），干两件事：
① 注入 `proxy-server` 启动参数；② 把直播伴侣自己的**防篡改校验**替换成 `if(false)` 屏蔽掉。

问题出在第②步——它用两条写死的正则去匹配校验代码：

```csharp
new Regex(@"if\(\w{1,2}\(\w\),!\w\.ok\)")
new Regex(@"if\(\(0,\w.integrityCheckReport\)\(\w\),!\w\.ok\)")
```

**这两条跟当前版本（12.6.4.431952921）的代码对不上**。实测那份被改过的 `index.js`：
`proxy-server` 注入成功了，但 `if(false)` 出现 **0 次**、`integrityCheckReport` **原封不动**。
于是变成"文件被改了，而校验还活着"——直播伴侣一启动就判定自己损坏。

⏱ **为什么 07-30 改的、08-04 才炸**：文件是 07-30 11:37 改的（第一次跑开播模式那天），
中间 08-01、08-02 两场都正常。所以那个完整性校验**不是每次启动都跑**（可能定期、可能由某个
服务端开关触发）。**这条最坑**：改坏和发作之间隔了 5 天、隔了两场成功的直播，很难联想到一起。

**已处理**：
1. 用抓包程序自己留的 `index.js.bak`（07-22 原版，已验证干净：无注入、校验完好）还原了
   `index.js`。被改过的那份留作证据：`index.js.被抓包程序改过-20260804`。
2. `liveCompanHookSwitch` 改成 **false**，防止它下次启动再注入一遍。**别改回 true。**

**对弹幕的影响（有不确定性，要实测）**：注入没了之后，直播伴侣不再被强制指向抓包程序的代理。
但 `sysProxy=true` 仍然生效（抓包程序照样改系统代理），而**直播伴侣是 Electron 应用、默认跟随
系统代理**，所以它的流量**可能仍然被拦得到**——这一点没有实测过，不能打包票。真正的兜底是
08-03 做的**弹幕备用源**：桌宠自己那个窗口是自己 `setProxy` 的，完全不依赖这个注入，一定能工作。

⚠️ **中控台的失误**：08-03 读 `LiveCompanHelper.cs` 时**看到过** `SetSwitch("proxy-server", ...)`
这段，但当时只顾着找"重启会不会掉播"的答案，**没有意识到它是在改写直播伴侣的安装文件、
更没有把这个风险告诉主播**。教训：读第三方代码时，看到"往目标程序目录里写文件"这类动作，
无论当下在查什么问题，都要当场标记成风险并告知——它的后果不会立刻出现，但一定会出现。

## 契约与集成

- ⚠️ **`command.calibrate` 只有发送端，没有接收端**（2026-07-30 核查）：`apps/control-panel/commands.py:35` 能发这条命令，但 `perception-game/run.py` 只订阅了 walkthrough、`perception-face/run.py` 压根不订阅总线消息，**谁都没在处理 calibrate**。两个服务的源码注释都写着"区域由 `command.calibrate` 提供"，实际上区域只能靠启动时的 `--region` 命令行参数传，而 `main.js` 起 game 时没传任何区域 → **实际采集范围是整个主显示器**。所以待办里的"竖屏/横屏区域校准"现状是"面板能发、没人收"。
- 事件契约（`packages/contract/`）：三频道 `perception.* / command.* / action.*`，信封 `{channel,type,ts,source,data}`，`validate.py` 校验。✅ 稳定。2026-07-29 新增 `perception.audio.self_speaking`（`{on: bool}`）——桌宠自己正在出声，用于出声期间掐掉麦克风、断掉"她听见自己→自问自答"的死循环。⚠️ 这是**表现层往 `perception.*` 发布**的唯一一处，理由与两侧超时兜底的要求写在 `events.md` 对应小节，改之前先读那段。
- 常驻 TTS（`services/tts/tts_daemon.py`）：替掉"每句现起一个 edge-tts 进程"，省掉每句约 0.86s 的解释器冷启动。✅ `verify_tts.py` 6/6（**唯一需要联网的验证脚本**，没网/没依赖时自动 SKIP 而不是 FAIL）。起不来、出错或超时会自动退回原来的命令行方式，所以它坏了只会变慢，不会让桌宠说不出话。
- 本地事件总线（`services/bus/`）：JSON-lines over TCP，Python 服务与 Electron 角色均连接，默认 `127.0.0.1:8765`。✅ 真实 TCP 端到端验证通过，且跨 Python/Node 运行时验证通过（`scripts/verify_integration.py`、`scripts/verify_xruntime.py`）。

## 六大能力

| 能力 | 状态 | 验证方式 |
|---|---|---|
| 看游戏·被吓即时反应 | ✅ 真实可用 | `services/perception-game/verify_game.py`：暗/亮帧亮度、防连发、端到端合成恐怖帧→桌宠 scared |
| 看游戏·卡关攻略（含联网搜索） | ✅ 真实跑通，**2026-07-30 接入真实联网搜索** | `services/perception-game/verify_vision.py` + `verify_walkthrough.py`：看画面 + 自动认游戏名/主播补充 + 真实浏览器搜索 + 只用搜到材料回答（搜不到如实说没搜到，绝不伪造来源）。详见 CHANGELOG 上方"卡关攻略接入真实联网搜索"一节 |
| 看露脸·表情调侃/夸夸 | ⏸️ **2026-08-01 用户决定暂不启用**（不删代码，以后可能恢复） | 算法本身 `services/perception-face/verify_face.py` 真实验证过（用真实照片端到端跑通，含修复 Windows 非 ASCII 路径导致 OpenCV 人脸检测失效的 bug，见 DEVELOPMENT.md），但一直没接进一键启动——`perception-face` 不在 `apps/character/main.js` 的 `SERVICES` 列表里，只在 `INTEGRATION.md`/`start-pet.cmd` 里作为"手动可以这么跑"出现过，从未随桌宠自动起过。2026-07-30 核查还发现两个更深的问题，留着供以后恢复时参考：① 它默认截**整个主显示器**，而主播的脸来自摄像头、进的是直播伴侣，根本不在自己屏幕上，就算跑起来也看不到脸；② `layout`(portrait/landscape) 不做任何裁剪，只是塞进事件里的标签字段。要真恢复这项能力，得先决定脸从哪来（直接读摄像头 vs 截直播伴侣预览窗）。**当前状态**：代码原样保留在 `services/perception-face/`，不启动、不修复、不再计入可用能力，跟"看插件·OCR"是同一类处理，但那个是永久弃用、这个是暂停——想恢复时脸从哪来这个设计问题仍然要先决定。 |
| 看弹幕·欢迎/礼物分级/昵称/回复分层 | ✅ **2026-07-30 真开播抓包链路验证成功，且离线重放验证反应链已通** | 实时数据源已确定用 DouyinBarrageGrab（见顶部）。真实数据的 `Data` 字段是 JSON 字符串这个坑已修（原先 200 条全静默失败），拿录制的 908 条重放：907 条成功。回复按身份分层 + 语音配速（VIP 必回不出声降到每 3 分钟一次）已落地。**⚠️ 尚未真开播验证过这批反应链**，只验证过抓包本身和离线重放，见 CHANGELOG 上方"弹幕反应、回复分层…"一节。"求礼物"能力已决定移除（诱导送礼合规红线），brain.py/perception-voice/control-panel/character 四处均已清理完毕 |
| ~~看插件·倒计时/整蛊~~ | 🚫 **2026-07-29 用户决定弃用** | 代码仍在 `services/perception-ocr/`，当年真验证过；但 `rapidocr-onnxruntime` 后来从环境消失（requirements 列着、实际没装），且该能力属边缘价值。不修复、不启动、不再计入可用能力。要恢复只需装回依赖 + 跑 `verify_ocr.py`，代码没动过 |
| 控制·语音(仅主播声纹)/面板/弹幕关键词 | ✅ 真实可用 | `services/perception-voice/verify_voice.py`：真实录音声纹注册+识别端到端；`apps/control-panel/verify_control_panel.py`：面板/关键词端到端，弹幕关键词现在要求昵称在白名单内才生效 |
| 角色形象+动作+语音气泡 | ✅ 真实可用，已过用户验收；阶段B(Live2D/神经动画)可选升级 | `apps/character/verify_character.js`：action-map 映射 + edge-tts 出声。AI 生成透明背景逐帧插画（108 帧/13 状态，含 `idle_sleep` 环境待机），成套表情/动作/气泡齐全，2026-07-23 用户已亲自验收确认无问题；同日给 `renderer.js` 加了语音排队播放（多句 speak 几乎同时到达时排队串行，不互相打断），`test_renderer.js` 覆盖；Live2D 或神经单图动画是非 demo 必需的可选进一步升级，需要美术方向输入 |

## 待办

### ~~当前目标：录制提案 Demo~~（2026-07-14 定，**已随 07-26 方向变更作废**）

提案路线已关闭，下面这一节只作为历史记录保留——里面 5 项开发前置工作都真的做完了且仍然有效，但"提案评估/提交材料"这个目标本身不再成立。当前目标见顶部「实时弹幕数据源」。

~~抖音「直播玩法」提案评估需要一份 demo 视频 + 文案说明，这是解锁点赞/礼物/评论真实数据权限的前提。~~

**2026-07-23 状态**：下面 5 项开发前置工作全部完成（3个 worktree 已合并进 main）。剩下唯一的大头是 demo 本体——录像 + 飞书提案文档，这两件事完全没开始，跟前面的开发工作性质不同（一个是写代码，一个是产出内容），是接下来唯一要做的事。录 demo 前曾需要先补齐的 5 项：

1. **立绘/美术质量提升**：✅ 已完成（2026-07-15，`feature/character-look`）——原手绘 SVG 占位换成 AI 生成透明背景逐帧插画，13 状态/108 帧。
2. **动作反应更生动**：✅ 已完成（同上）——12 个原状态全部升级为真实逐帧动画（不再是整图 CSS 晃动），新增 `idle_sleep` 环境待机；`beg` 动作保留（wink+爱心+"爱你哟！"），改触发关注答谢，不再挂靠已移除的求礼物话术。
3. **可测试/可直播用的 UI**：✅ 已完成（`feature/control-ui`）——就用现有 `apps/control-panel`，加了观众分级面板（自动抓取本场出现过的昵称，下拉选普通/会员/星守护，发 `command.set_viewer_tier`）+ 开播/下播按钮（发 `command.stream_start`/`stream_end`）+ 语音识别开关壳子，没有另起一个。`verify_control_panel.py` 14/14 全过。
4. ~~语音音色升级~~ **demo 阶段已解决，不阻塞**（`feature/voice-tone` worktree 结论，2026-07-22）：调研后决定 demo 先用现有默认音色 `zh-CN-XiaoyiNeural`，未改动任何项目代码（`verify_character.js` 7/7 保持通过）。真正的根因不是"选错了音色"，而是**`emotion` 字段在渲染链路中途丢失**——`brain.py` 算出的 `emotion` 一路传到 `action-map.js`，但在 `renderer.js → preload.js → main.js` 这三层被丢弃，从没真正传给 edge-tts 调用，所以听起来才没有感情起伏。本地部署 CosyVoice/EmotiVoice 这类情感TTS引擎调研过，工程量不小（要装ML环境+下大模型权重，且当次调研撞上了本机 Shadowsocks 代理导致 git/pip/gh 联网命令失败的已知环境坑），已决定暂缓，移到下方"语音升级"一并处理，不算 demo 待办。
5. **LLM 对话服务**（2026-07-14 新增，`services/dialogue/`，`feature/dialogue-service` worktree）：让桌宠能跟弹幕/主播真聊天，不再是纯模板台词。⏳ **2026-07-22：骨架+离线验证已完成**，**2026-07-23：并发架构 + 分级录入解耦/开播下播生命周期均已完成**——LLM 调用挪进后台 worker 队列，`handle()` 不再同步等 DeepSeek，总线接收线程不会被卡住；`command.set_viewer_tier`/`stream_start`/`stream_end` 三个新事件也已接上（分级录入写自己的 viewers 表、开播清空本场状态、下播先按"来过≥3场/跨场送礼够多/本来就是member或star_guardian"判定常客写结构化 note 再清零单场计数）（`python services/dialogue/verify_dialogue.py` 53/53，含故意把调用调慢 1.5s 断言仍不阻塞、以及三种常客判定条件各自的用例）；`services/perception-voice/intents.py` 联动扩展也已完成（`verify_voice.py` 18/18）；顺带给 `services/bus/bus_client.py` 的 `publish()` 加了锁（新架构下会有两个线程并发发布）；实现过程中还发现并修复两处真实 bug——① SPEC.md 原判断"单 worker 不需要给 `memory.py` 加锁"是不完整的（`verify_dialogue.py` 连跑几次复现了 `sqlite3.OperationalError`），总线线程和 worker 线程会并发摸同一个 sqlite3 连接，跟 worker 数量无关，已加 `self._db_lock` 修复；② DeepSeek `deepseek-v4-flash` 的 thinking 开关官方默认 enabled，不显式关掉会真实消耗 `reasoning_tokens`，实测最坏能把 `max_tokens` 预算全吃光、正文变成空字符串，已在 `chat.py` 加 `extra_body={"thinking": {"type": "disabled"}}` 关掉，详见 SPEC.md 里的"订正"。**2026-07-23：`PET_CHAT_*`（DeepSeek key）已配置真实 key 并验证通过**，真实调用成功拿到角色口吻回复。`apps/character` 排队播放已完成（2026-07-23，见"角色形象+动作+语音气泡"一行）。待办：主播的"我的习惯"（`streamer_notes`）还没做、`perception-voice/run.py` 真实麦克风循环仍未接线。详细设计见 `services/dialogue/SPEC.md`。核心结论：
   - 跟 `brain` 是平级订阅关系，不是上下级——两者都直接订阅总线上的 `perception.danmaku.*`/`command.*`，各自独立判断要不要反应，不新增"请求"类事件，契约不用改。
   - 触发规则：①主播喊"看一下弹幕帮我回一下"→抓最近10条弹幕依次回复；②星守护/会员（手动名单，权重高）；③本场送礼前三名；④默认常开「跟主播语音聊天」（唤醒词见下）。以上都是**叠加**在 brain 现有的即时模板反应之上，不是替换——比如送礼答谢，brain 的即时动作+模板答谢照旧瞬间触发，dialogue 生成的个性化回复是稍后追加的。
   - 语音唤醒词"魔丸魔丸"（主播起的桌宠名）+ 声纹校验，防止跟弹幕聊天时误触发；触发"卡关攻略"需要二次确认（"卡关啦？需要我来提供攻略帮助吗？"，用户答"是"才真正触发 vision.py 的 LLM 分析，答别的就用一句兜底话术带过），跟现有自动卡关检测并存不互斥。
   - 记忆分三层，SQLite 落地（不需要 FTS5/向量检索，规模小用不上）：①人设/主播档案——写死的 prompt 模板常量；②本场弹幕缓冲——给"看一下弹幕"批量模式用；③观众画像表——分级/本场送礼/上次出现/简短印象，回复前查一条、回复后更一条。
   - 模型：DeepSeek `deepseek-v4-flash`（非思考模式，$0.14/1M输入token，够用够便宜，不用 v4-pro）。
   - **合规护栏是硬性前提**：LLM 生成的文本必须过一遍禁用词清单检查（复用 `提案Demo自查清单.md` 里那份），不过关就换安全兜底模板再念出来，不能只靠 prompt 要求模型自律——这是从"纯模板"变成"弹幕内容能影响生成文字"之后新出现的风险，之前的固定台词系统不存在这个问题。
   - 失败兜底：DeepSeek 调用超时/出错，参照 `vision.py` 现有做法，退回角色口吻安全模板，不报错不卡死。
   - 星守护/会员名单录入界面放在 `apps/control-panel`（`feature/control-ui` worktree），不在 dialogue 自己做 UI；界面从"本场已出现的观众昵称"里勾选，不让主播手打字（防输错），选完发新增的 `command.set_viewer_tier`。
   - （2026-07-22 补充，详见 SPEC.md）契约新增两个事件：`command.set_viewer_tier`（分级录入解耦）、`command.stream_start`/`stream_end`（开播/下播手动信号，下播时清空本场计数+把常客信息沉淀进跨场记忆）。另外发现"看一下弹幕"批量模式会因为总线单线程顺序处理而卡住整条总线，方案是把 LLM 调用挪到后台 worker 线程，不在总线接收线程里同步等。

### 其余待办

1. **实时弹幕接入**：~~注册应用拿 AppID + 申请「互动数据」能力~~ 已改走 DouyinBarrageGrab 路线，归一化层与验证工具均已就绪，**剩下的是安装抓包程序 + 接真流量验证**，详见顶部「实时弹幕数据源：已选定路线」。（历史信息：AppID `宠物助手` `tt037b75c69d1eea8210` 仍在，`获取密钥与APPID.md` 只剩参考价值。）
2. ✅ ~~**语音实时循环**：`services/perception-voice/run.py` 的运行时循环尚未接线，执行到最后一行直接 `raise NotImplementedError`~~ **已完成，这条早就过期了（2026-08-11 核查订正）**。

   **📌 订正**：这条待办的描述从 2026-07-26 起就不再成立，但一直没人回来改。实际情况：`c3c270f`（07-26，"补上实时监听循环，语音识别可用"）就把循环接上了，此后 07-29 / 07-30 还做过多轮**真人语音实测**（自问自答死循环、模型预热、常驻 TTS 都是在那几次实测里定位修复的，见上方"语音升级 5b"一节）。2026-08-11 直接核查 `run.py`：**326 行，`NotImplementedError` 一个都没有**，有真实的 `while True` 采集循环、`_warmup()` 模型预热、`SpeakGate` 掐麦门。四个依赖（sounddevice / webrtcvad / faster-whisper / resemblyzer）也早就装好了。**这正是「环境漂移会让文档失真」那类问题的又一例**——代码前进了，待办条目留在原地。

   **⚠️ 面板现状（2026-07-26 实测的说法，2026-08-11 已部分失效）**：原文说点"开启语音识别"会立刻 `NotImplementedError` 退出、"开播/下播"按钮和观众分级下拉发给 dialogue 的三个方法都 `return []` 没有任何效果。**现在这两条都不对了**：语音识别真的能用（见上）；`_stream_start` / `_stream_end` / `_set_viewer_tier` 都已是真实实现，其中 `_stream_end` 会沉淀本场露过面的观众、判定常客、并触发主播档案总结——**控制台的「手动下播」按钮是这条链唯一的触发入口**（见上方"下播档案从没真正触发过的真因"一节）。仍然成立的那部分：动作区 7 个（挥手/被吓/大笑/谢礼/答谢/夸夸/撒娇，走 `command.do`）+ 状态区 4 个（闭嘴/恢复/休息/唤醒，走 `command.mute|unmute|sleep|wake`）是由 brain 的 `_command` 直接处理、有即时画面效果的。

   **📌 订正一条此前的误记**：这条待办原先写"当前缺 `voiceprint.npy`，需要主播重新录 6 段音频到 `enroll/` 目录重新注册"——**不成立**。该文件在主目录一直存在（`services/perception-voice/voiceprint.npy`，1152 字节，2026-07-12 生成）。它被 `.gitignore:22` 排除所以进不了 git，当时在别的 worktree 里看不到它就误判成"缺失"。接线时先拿现有这份试，不用重新 enroll。**这是"gitignore 掉的资源在 worktree 之间看不见"这类坑的第二例**（第一例是 `assets/character/*.png` 那 108 张立绘），判断某个 gitignored 文件"缺失"之前，先去主目录实际看一眼。
3. **抖音玩法提审合规收敛**：礼物映射收敛到平台礼物池选 ≤8 款；弹幕/评论指令词 ≤10 个；另需过一遍官方禁用词清单（游戏、玩家、抽奖、盲盒、礼盒、BUFF、HP、PVP 等）——这份清单现在也是 dialogue 服务运行时护栏要用的，一份清单两个用途。"求礼物"不再是"改写话术"，已改为**直接移除**（见上方六大能力表格的标注），**四处均已清理完毕**：`services/brain/brain.py`（BEG_LINES/begging状态/tick里的求礼物块）、`services/perception-voice/intents.py`（gift_begging intent）、`apps/control-panel`（求礼物按钮/CLI）、`apps/character`（`beg` 动作改触发关注答谢，不再挂靠已移除的 `gift_begging` 模式，气泡文案也已换掉）。细则见 `抖音玩法提审规则要点.md`。
4. **角色阶段B**（非 demo 必需，可选的更远期美术升级——当前 AI 逐帧动画已于 2026-07-23 经用户验收确认无问题）：升级为 Live2D 绑定或神经单图动画（藏在 `action.*` 契约后，不影响其它层）；`assets/character/gen_character.py` 可用国产文生图生成参考立绘（需先在阿里云百炼控制台开通对应 key 的图像模型权限）。
5. **语音升级（两件事，改的是同一段 TTS 代码，一起做省一次返工）**

   **5a. 没有感情**：根因是 `emotion` 字段在 `renderer.js→preload.js→main.js` 这三层被丢弃、从没送进 TTS 调用（见上方"当前目标"第4条），有两条路：① 先把现有 `emotion` 字段接通到 edge-tts 调用（edge-tts 本身支持 SSML 语气/风格参数），成本低，可能不用换引擎就有改善，建议先试这条；② 本地部署 CosyVoice/EmotiVoice 等情感TTS引擎换音色，成本高（要装ML环境+下大模型权重），且需要主播提供参考音频。

   **5b. 太慢 + 自问自答（2026-07-29 定位修复，07-30 真人复测通过）**：用户报"讲 3-5 句要等 2-3 分钟"，且"只说两句她回了一堆"。✅ **现在停口到出声约 6.2 秒、不再自问自答**（07-30 实测稳态：VAD 0.7s + 声纹 0.1s + 识别 2.0s + DeepSeek 1.4s + TTS 2.0s）。⚠️ 别拿"每句两三秒"当目标，那是中控台当时说大了——这五段没有一段能靠工程手段再砍一半，要大幅下降只能换本地 TTS + 更小的 ASR 模型，各有准确率/部署成本。真人跑一次后读 `apps/character/startup.log` 定位到三件事——

   - **自问自答死循环（真 bug，"回一堆"的原因）**：TTS 从音箱放出去被麦克风收回来，**声纹没挡住**（合成音相似度过了 0.60 阈值；声纹本来就是分"主播 vs 别人"、不是分"人 vs 机器"），被当成主播的新话又回一句，连续 5 轮直到用户杀进程。日志实证 `14:14:43` 说的话在 `14:14:49` 被自己听见。✅ 已修：新增契约事件 `perception.audio.self_speaking`（character 发、perception-voice 订阅），出声期间直接丢麦克风帧，两侧各有超时兜底防止麦克风永久失聪；`run.py:SpeakGate` + `verify_voice.py` 11 条用例。**07-30 复测通过**——`暂停收音`/`恢复收音` 成对出现且其后没有"听到：<她刚说的话>"，判断这条修好没有就看这一点。
   - **首次说话干等 37 秒（真 bug，"2~3 分钟"里对不上的那段）**：第一段 `声纹12.4s 识别24.7s`，之后每段只要 `0.0s + 1.6~2.1s`，差的全是懒加载。还有二次伤害：等不到回应用户会重说一遍，排队翻倍。✅ 已修：`run.py:_warmup()` 开麦前先空跑一次模型（两个模型并行加载，07-30 实测 `声纹14.7s 识别33.6s 实际等待33.6s`，串行本来要 48.3s），且预热完才提示"开始监听"。📌 预热耗时受系统文件缓存影响很大（同机实测过 12.4/28.7/14.7s 三种），"变慢了"不等于代码退化。
   - **每句固定 ~4s 合成开销**：✅ 压掉约 0.86s（新增常驻进程 `services/tts/tts_daemon.py`，替掉"每句现起一个 edge-tts"，失败自动退回原路径），并让排队的下一句在当前这句播放期间就合成（`renderer.js` prefetch）。07-30 复测稳态 **1991ms**（旧方式 4131ms）。⚠️ **踩过一次**：常驻进程最初做成"第一次说话才启动"，它自己那 0.86s 启动照样压在第一句上，实测第一句 4155ms 跟旧方式毫无差别、等于白做，已改成 `app.whenReady` 里就起——**任何"常驻/池化"优化都要检查预热有没有被懒加载抵消掉**。剩下约 2s 是到微软的网络往返，抖动 1.6~4.8s，**砍不掉**，要再压只能走 5a 的本地引擎。
   - **DeepSeek 首次调用 6.8s、第二句起 1.4s**（07-30 实测）：跟 TTS 同一类毛病，建连接的一次性开销，⏳ 未处理。

   ⚠️ **推翻一条此前的判断**：原先以为"拆成短句先说第一句"能把首字等待压到 2s。**实测证伪**——合成耗时跟文本长短几乎无关（4 字 3.06s / 36 字 2.78s），拆句只会把固定开销付好几遍，更慢。别再往这个方向改。
   📌 顺带测到未动：走 Shadowsocks 代理合成 2.2~4.3s、直连 2.5~3.2s，代理平均慢约 0.5s 且有长尾（出现过 12.6s）。没改成绕过代理——直连能通只是当下观测，网络一变可能整个 TTS 不通，属环境权衡，留给用户定。
   ⚠️ 曾怀疑"关掉 Shadowsocks 后 edge-tts 走死代理"，**实测已证伪**，别再往这个方向猜。
6. **实播彩排**：竖屏/横屏区域校准 + 真实弹幕/语音冒烟测试。✅ **采集定格已于 2026-07-30 解决**，见下方「桌宠窗口的直播采集」一节。
7. **玩法客户端(exe)技术对接**（提案通过、准备真实提审时才需要）：读取直播伴侣启动时传入的 `-token=xxx` 参数 → 调用官方"获取直播信息"OpenAPI 换 room_id；`apps/character/main.js` 加 `app.commandLine.appendSwitch('no-sandbox')`（官方文档记录的 Electron 已知 GPU 崩溃修复）。~~点赞（必接能力之一）目前没有对应的桌宠反应设计，需要补~~——2026-07-23 复核：`danmaku.like → praise` 反应链路（契约+brain.py+action-map.js）其实从早期 `4d88a42` 就有且测试通过，这条早就做了，之前这里的说法是文档没跟上；点赞/礼物/评论三者真正共同缺的只是「实时数据源接入」（见上方"实时弹幕接入"一条），不是点赞独有的缺口。

## 已知限制

- Electron 桌宠窗口的可视化验证在无真实桌面会话的环境里可能受限（`capturePage` 等 API 依赖可交互桌面）；真实 Windows 桌面下 `npm start` 正常。
- 露脸/语音的**识别准确率**是概率性的，不是保证；`services/perception-face/verify_face.py` 里如实记录过一次真实模型判断与人类直觉不符的样本，未强行"调"出符合预期的结果。
- 弹幕数据源依赖抖音开放平台，接口可能随官方更新变化；模块通过契约保持可替换。
