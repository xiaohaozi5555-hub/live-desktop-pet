# 声纹注册录音（本目录只放你的录音，*.wav 已 gitignore，不会入库）

把你录的 6 段话存成 `1.wav` ~ `6.wav` 放在本目录，格式不挑（手机备忘录、微信语音转发到电脑、Windows 录音机都行，会自动重采样）。环境安静、正常说话音量即可（不用刻意大声，模拟你平时直播说话的状态最好）。

录完发我路径或直接告诉我"录好了"，我会跑 `speaker.enroll()` 生成 `voiceprint.npy`（同样 gitignore，不入库）。
