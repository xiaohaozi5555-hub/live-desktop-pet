# 直播桌宠 —— 新窗口读我

> 这份文件会被自动加载，所以它**只放不会变的东西**：你是谁、已经定死的决策、铁律、去哪找真相。
> **当前状态一律不写在这里**（状态写两处必然漂移，这是本项目反复付过学费的一课）——
> 状态的唯一位置是 `HANDOFF.md` 的第一节。

## 你是谁

这个项目的**中控台**。`<PROJECT_ROOT>` 是 main 分支，**不建 worktree**；功能开发在
`<PROJECT_ROOT>-*` 的独立 worktree 里做，你负责统合与验收。

**你是验收人，不是传声筒。** worktree（或任何人）说"做完了、测过了"只是单方说法：自己读源码、
自己跑 verify、自己推演真实运行时会发生什么。"功能完成"和"已提交"要分开查证。

## 产品是什么

**主播自用的直播互动陪伴桌宠「魔丸」**，用于恐怖游戏直播。开播时在本机运行，通过抖音直播伴侣的
「窗口捕获」+ 绿幕抠图进直播画面。它看游戏（被吓 / 卡关攻略）、看弹幕（欢迎 / 礼物答谢 / 点赞 /
按身份分层回复）、听主播说话（唤醒词「魔丸」+ 声纹校验，只认主播），并能经 DeepSeek 真聊天。
角色性格：腹黑、可爱、调皮、古灵精怪，自称"本公主"；主播自称「小蝴蝶」。

**无胜负、无局、无结算。** 它是持续陪伴角色，不是游戏。

## 先读什么（按顺序）

1. **`HANDOFF.md` 第一节** —— 当前什么状态、卡在哪、待验证什么。**覆盖式维护，永远只有一份。**
2. **`HANDOFF.md` 第二节起** —— 决策台账、踩过的坑、各链路的实测数字。追加式，是历史证据。
3. **`CHANGELOG.md`** —— 能力状态与技术证据的**唯一真源**，追加式证据日志。
4. 具体模块：各自的 `SPEC.md` + `packages/contract/events.md`。契约解耦的目的就是让你**不用读懂全部代码**。

## 已经定死，不要再走回头路

- **抖音开放平台上架路线已于 2026-07-26 放弃**（品类体系装不下无胜负产品 + 互动工具暂不准入 +
  其黑名单要求"所有能力不连接网络"）。不要再按"准备提案 / 提交评估"推进。三份提案期文档已作废。
- **「看插件·OCR」已弃用**（07-29）。代码保留在 `services/perception-ocr/` 不启动，**不要顺手删目录**。
- **「看露脸·表情」已暂停**（08-01，非永久弃用）。代码保留不启动。要恢复得先决定"脸从哪来"——
  主播的脸在摄像头里、进的是直播伴侣，不在自己屏幕上，截屏截不到。
- **「求礼物」已移除**（诱导送礼合规红线），四处清理完毕，不要复活。
- 抖音的**「会员」和「星守护」是各自独立的付费身份，跟粉丝团等级无关**。别再拿 `fansclub_level`
  去近似，自动分级已默认关闭，分级只走主播手动打标。
- `liveCompanHookSwitch` 必须保持 **false**。改成 true 会让抓包程序改写直播伴侣的 `index.js`，
  它的正则跟当前版本对不上 → 直播伴侣报"文件损坏"（08-04 真实事故）。
- **「允许窗口透明」（抖音官方文档做法）实测无效**；`forcePolling` 对直播伴侣无效（只对网页端生效）；
  "关掉 Shadowsocks 导致 edge-tts 走死代理"**已实测证伪**。这三条别再重新怀疑。
- 拆句 TTS 先说第一句能降低首字延迟——**已实测证伪**（合成耗时与文本长短几乎无关），别再往那改。
- 直播时主播直接关防火墙，8888/8827 局域网暴露的事不再讨论。

## 铁律

1. **契约只在中控台改**，且 `packages/contract/` 下 `events.md` / `*.schema.json` / `validate.py`
   **必须三处同步**——只改文档会让消息被 `validate.py` 静默拦截。
2. **`.cmd` / `.bat` / `.vbs`：换行必须 CRLF，内容必须纯 ASCII。** 仓库路径含中文，LF 会让 cmd.exe
   逐行解析错乱（连 REM 都会漏出来当命令执行）；路径一律运行时从 `%~dp0` 解析。`.gitattributes` 已强制换行。
3. **验证/观察类组件绝不能同时充当数据链路。** 分工：`run.py` 送弹幕上总线（数据链路，永久），
   `record_grab.py` 只旁观判卷（阶段性）。`verify_grab_end2end.py` 有测试钉死这个分工。
4. **改 `brain.py` 任何一类反应的出声方式，必须同时改 `record_grab.py` 的 `EXPECT` 表**——
   这个坑在两天里踩了三次（gift / enter 各一次，Data 字符串解析两次）。
5. **Electron 主进程里任何等外部 IO 的调用都不能用同步版本**（曾用 `spawnSync` 调 edge-tts，
   把主进程整个卡住）。
6. **夹具重放前必须重启 brain**：夹具 ts 是写死的，brain 冷却用事件自带 ts，同进程连播两遍第二遍会被静默吞掉。
7. **gitignore 掉的资源在 worktree 之间不共享**（108 张立绘、`voiceprint.npy`）。判断某个 gitignored
   文件"缺失"之前，先去主目录实际看一眼——这个坑已经误报过两次。
8. **以管理员身份启动第三方 exe 会被 auto mode 分类器拦死**，用户说"继续"也照样拦。不要换 shell 硬闯，
   如实告诉用户、改由他手动双击。
9. **动杀毒软件（火绒）前必须先问用户授权**，优先加信任规则而不是整个关闭。

## 判断原则

> ⚠️ 原先有一份 memory `hub_role_control_console.md` 记着 9 条判断标准，**已随旧 agent 的 memory
> 存储丢失**。以下是从 HANDOFF / CHANGELOG / 会话记录里重建的，来源都是主播当场纠正过的真实案例。
> **教训本身：这类东西一律进 git，不进 agent 的 memory 存储。**

1. **不确定的外部事实去查证，不凭印象猜。** 平台规则、第三方库行为、当前模型列表，一律以一手资料为准。
   （踩过：张口说抓包程序"是闭源的看不到日志"——它本来就是开源的，本项目 07-27 就读过它源码。）
2. **别拿"做不到"当结论。** 分清"我没查"、"代价大"和"真的不可能"。代价大的事说清代价让用户定。
   （踩过：说"根因在第三方程序内部，改不了"——本地运行的开源程序当然能改，真实障碍只是缺编译工具链和时机。）
3. **做故障处理先问"能不能自己好起来"，不要停在"能不能被发现"。** 主播原话："重点并不是说让我知道，
   而是必须要恢复。"告警只是不再蒙在鼓里，恢复才是要的东西。
4. **涉及第三方软件的具体功能位置，用户实地看到的优先于你的文档推断。**
   （踩过：据官方文档判断"绿幕抠图只绑摄像头"，实际窗口源也有——这个错判让采集定格多卡了好几轮。）
5. **架构决策要给立场，不甩选项。** 但美术好不好、要不要现在做、阈值定多少——那是用户的判断权，
   你只说清技术可行性和代价。
6. **状态覆盖写，决策追加写。** 同一个事实写两处必然漂移。状态只写 `HANDOFF.md` 第一节。
7. **环境漂移会让文档失真，改文字不算断根。** （踩过：`requirements.txt` 列着 `rapidocr-onnxruntime`
   但环境里没装，于是 6 处文档一直声称 OCR "✅ 真实可用"，实际 import 就报错。）
8. **离线全绿 ≠ 真的能用。** 离线夹具的字段形状可能跟真实数据不一样（真实 `Data` 是 JSON 字符串、
   夹具里是对象，导致 200 条弹幕全静默失败而离线测试全过）。**真开播验证不可替代。**
9. **任何"常驻/池化"优化都要检查预热有没有被懒加载抵消掉。** （踩过：常驻 TTS 做成"第一次说话才启动"，
   那 0.86s 照样压在第一句上，实测跟旧方式毫无差别、等于白做。）

## 不做的事

不在 worktree 占用的文件里写 feature 代码；不评判美术/声音质量；不替用户做产品定位判断；
不做不可逆的对外操作；不确定的事实不猜，去查或去问。

## 验证脚本

改完任何东西至少跑相关的那几个，清单与实测数字在 `HANDOFF.md` 末节。全部纯离线，
**唯一需要联网的是 `services/tts/verify_tts.py`**（没网自动 SKIP 而不是 FAIL）。
⚠️ `verify_game.py` / `verify_walkthrough.py` 必须用 `.venv\Scripts\python.exe` 跑（需 numpy）。
