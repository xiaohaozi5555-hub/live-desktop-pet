# HANDOFF —— 中控台交接

> **这份文件的维护规则（2026-08-11 定，请照着做）**
>
> - **第一节「当前状态」是覆盖式的**：情况变了就直接把它改掉，**不许在顶部追加时间戳更新块**。
>   全项目的"当前状态"只有这一份。
> - **第二节起是追加式的**：决策、实测数字、踩过的坑——这些是历史证据，不会过期，只增不改。
> - 能力状态与技术证据的完整记录在 `CHANGELOG.md`（唯一真源），这里不复制，只引用。
> - 你是谁 / 产品是什么 / 铁律 / 已经定死不许回头的决策 → `CLAUDE.md`（自动加载，这里不重复）。
>
> 📌 **为什么定这条规则**：2026-08-10 主播的对话窗口全丢了，恢复认知花掉一整个窗口。复盘发现
> 病根不是文档写得不够（密度已远超正常项目），而是**用追加的方式写状态**——顶部曾叠着 6 个
> 时间戳更新块（08-01 / 08-01 晚 / 08-01 深夜 / 08-02 / 08-03 / 08-04），而文件头还写着
> "最后更新 07-30"、第三节的开播顺序早已过期。"当前状态"被摊在七个地方并且互相矛盾。
> 最贵的一课：**08-04 修正后的开播顺序当时只活在聊天窗口里，仓库里一个字都没有**，
> 是后来从 jsonl 备份挖回来的（`scripts/extract_session_history.py`）。
> 决策和证据可以追加（它们是历史，不会过期），**状态必须覆盖**。

## 一、当前状态（覆盖式，全项目只有这一份）

**最后更新：2026-08-11**，由"恢复认知"那个窗口重写。**上一次真开播是 08-02。**

### 现在卡在哪

**唯一主线：08-03 / 08-04 那批改动一次真播都没跑过。** 即弹幕备用源、断流自动恢复，以及
08-04 事故后把 `liveCompanHookSwitch` 关掉之后"直播伴侣的流量还抓不抓得到"这个问题。
这些全部只有**源码推理 + 离线验证**。

**在验过之前不要叠新功能**——没验过真实效果就往上加，只会让问题更难定位。

`.cache/grab/` 实际核对过的真开播记录：

| 日期 | 录制 | 结果 |
|---|---|---|
| 07-30 | `raw-20260730-113707`，908 条 | 抓包链路成功，但桌宠对弹幕毫无反应（`Data` 是 JSON 字符串没解析，真 bug，已修） |
| 08-01 ① | 无有效录制 | 中途下播：进场欢迎语音刷屏太吵（已改成文字气泡 + 欢迎窗口） |
| 08-01 ② | `raw-20260801-114712`，255 条 / 1486 秒 | 验证员报"礼物 0 反应"，查明是**验证员自己判卷 bug**，桌宠逻辑是对的 |
| 08-02 | `raw-20260802-111449`，29 条 / 76 分钟 | **弹幕开播 19 分钟后断流 58 分钟**（→ 08-03 做了备用源 + 自动恢复） |
| 08-03 起 | 无 | ⚠️ 至今没有再真开播过 |

### 开播 / 下播顺序

**不在这里写第二份。** `开播模式.cmd` 跑完会自己把完整顺序打印出来（开播 6 步 + 下播 3 步）——
它和真实启动流程在同一个文件里，改一个必然改另一个，没法各走各的。这里只记两条最容易错的：

1. **先开播模式，后开直播伴侣。** 抓包程序要先把系统代理架起来，直播伴侣才能在代理就位**之后**
   去建那条弹幕连接。反了，那条连接是直连的，抓包程序永远看不见。
   （08-04 起**不需要**再"完全退出直播伴侣重开"了——那个要求的理由是让它读到被注入的启动参数，
   注入已经关掉，理由不存在了。）
2. **下播先在控制台点「手动下播」。** 这是下播档案总结的**唯一**触发入口，直接关窗口会静默跳过
   ——前几场真开播档案一直是空的就是这个原因。

### 待验证清单

**🔴 A 组：08-03 / 08-04 做的，一次没验过（最高优先级）**

1. **弹幕备用源到底工作不工作** —— 看 `apps/character/startup.log` 搜 `[backup]`。
2. **注入关掉后，直播伴侣的流量还抓不抓得到** —— `sysProxy=true` 仍然生效，而直播伴侣本身是
   Electron 应用、默认跟随系统代理，所以**大概率还能抓到，但没实测过，不能打包票**。
   真正的兜底是备用源（它自己 `setProxy`，跟这一切无关）。
3. **断流自动恢复真能救回来吗** —— 判断方式：告警之后有没有自动跟上一条"✔ 弹幕恢复了"。
   ⚠️ 现在有两个都能解释断流的假设（① WS 端点正则不匹配 ② Chromium 把代理标记为坏、回退直连），
   从已有数据分辨不了。**假设 ② 下"重启抓包程序"这条恢复路径救不回来**，只有备用源可靠。
4. **控制台「手动下播」按钮的点击效果** —— 代码改完了，但当时怕打断正在跑的长重放测试，
   **没重启桌宠验证过**。

**🟡 B 组：07-30 ~ 08-01 做的，只有离线验证**

5. **进场欢迎的节奏** —— `WELCOME_CYCLE_MS`(10 分钟) 只开放 `WELCOME_WINDOW_MS`(1 分钟)。
   这两个数字**没有真实数据支撑**，是照搬"每 3 分钟一次语音"的经验定的，重点听感觉对不对，
   不对就是改这两个常量的事（`services/brain/brain.py`）。
6. **主播语音对话 / 记忆这条链** —— 至今一次真实语音都没测过，其中包含 08-01 深夜砍掉
   "喊一次魔丸免唤醒续聊 18 秒"、改成**每句都要喊唤醒词**这个改动。
7. **下播档案总结像不像主播本人** —— 看 `services/dialogue/dialogue_memory.db` 的
   `streamer_profile` 表。提示词（`streamer_profile.build_summary_prompt`）大概率要按实际效果调。
8. **完整重启后的第一句 TTS 耗时** —— 期望 ~2s 而不是 4.2s。07-30 那次主播只重启了语音服务、
   没重启 Electron，所以"开机就起常驻 TTS"这个改动还没被验证过。
9. 主动互动尬不尬（`PROACTIVE_INTERVAL_MS`）/ 气泡好不好读 / 卡关求助的真实体验 /
   **送礼金额够大绕过限流出声这条分支**（离线重放窗口内一次都没触发到，代码读过没问题但缺真实样本）。

**🔵 C 组：待主播拍板的开放问题**

10. **礼物连击 `GiftCount` / `RepeatCount` 不总相等** —— `normalize.py` 现在取两者较大值当
    `count`，如果 `RepeatCount` 其实是"连击进行中的实时计数"，一次 9 连击会被算成
    1+2+…+9=45 个礼物单位、送礼额多算 5 倍，会实际影响 `VIP_SESSION_COINS`(>1000) /
    `GIFT_SPEAK_MIN_COINS`(≥10) 的判定。**主播 08-01 明确说这个先不管、不要主动再提**，真要查再说。
11. 两个产品判断，主播说"先别急"但已提出，需要在合适时机拍板：① 闭嘴恢复后要不要延迟一会儿
    再触发主动互动（现在冷却计时器不重置，可能一恢复就蹦一句"点关注"）；② 被吓到
    （`game.scare`）时要不要暂停主动互动，避免打断被吓的反应节奏。

### 环境事实快照（2026-08-11 实地核对过，不是转述文档）

- ✅ **直播伴侣已干净还原**：`index.js` 与 07-22 原版 `index.js.bak` **字节完全一致**，
  `proxy-server` 注入 0 处、`integrityCheckReport` 校验完好。被改过的那份留作证据
  （`index.js.被抓包程序改过-20260804`）。版本 `12.6.4.431952921`。
- ✅ **抓包程序配置**（`D:\BarrageGrab\WssBarrageServer.exe.config`）：
  `processFilter=直播伴侣,electron`（`electron` 是给弹幕备用源加的，保留了`直播伴侣`）、
  `liveCompanHookSwitch=false`（**别改回 true**）、`sysProxy=true`、`forcePolling=false`、
  `listenAny=false`（⚠️ 该配置项在 v2.8.0 里是死的，8888/8827 照样对局域网开放）、
  `upstreamProxy=""`（**空着是对的**，2026-08-11 主播拍板：开播时 SS 关着，见「坑」一节）。
  备份：`WssBarrageServer.exe.config.bak-20260803`。
- ⚠️ **改过的配置可能从没真正生效过**：配置 mtime 是 08-04 11:22，而 `开播模式.cmd` 发现抓包
  程序已经在跑时会**直接复用、不重启它**。脚本现在会在复用时打印警告，但要让配置生效，
  得让开播模式自己去启动它（或改配置后确认它没在跑）。
- ✅ **git 干净**：无未提交改动；4 个 `<PROJECT_ROOT>-*` worktree 的分支**全部 0 commits ahead of
  main**（7 月留下的空槽，可以继续当 worktree 用，里面没有独有工作）。
- 🔴 **已丢失且无法恢复**：旧 agent memory 里的三份文件——`hub_role_control_console.md`
  （9 条判断标准）、`douyin_proposal_demo_plan.md`、`feedback_observer_not_in_datapath.md`。
  已从 HANDOFF / CHANGELOG / 会话记录里重建出一份判断原则写进 `CLAUDE.md`，
  但**不保证等于原来那 9 条**。**教训：这类东西一律进 git，不进 agent 的 memory 存储。**

### 还没做、但已经想清楚该怎么做的

**`scripts/preflight.py` 开播前自检**（建议单开一个窗口做）。把上面「环境事实快照」那一节
人肉核对的事情写成一个能跑的脚本，输出红绿表：抓包程序配置各项、**直播伴侣 `index.js` 的 hash
是否仍等于 `.bak`**、抓包程序是否已在跑、`.venv`/端口/`voiceprint.npy`/BOYA 麦克风、最近一次
真开播是哪天、顺手跑一遍 verify 套件打印真实数字。

其中 `index.js` 的 hash 检查单独就值回票价：08-04 那次事故是 **07-30 改坏、08-04 才发作**，
中间隔了 5 天和两场成功的直播（那个完整性校验不是每次启动都跑）。有这个检查，改坏的当天就会红。

## 二、深挖记录（追加式：根因与实测数字，不会过期）

### 采集定格：2026-07-30 已解决

**能用的配置（实测走通，桌宠干净地悬浮在直播画面上）**：

```
桌宠：不透明窗 + 纯绿底(#00FF00) + 关硬件加速
直播伴侣：「窗口捕获」源  →  对该源开「绿幕抠图」
```

**开播模式.cmd 已经默认这么起了**，主播不用记任何参数。控制台「直播采集」也有切换按钮。

**根因是两层，缺一层都不行**（这也是为什么之前一直没治好——每次只碰了一层）：

| 组合 | 结果 |
|---|---|
| 透明窗 + 窗口捕获 | **定格**。透明窗在 Windows 上是分层窗口，逐窗口采集拿不到新帧 |
| 不透明 + GPU 合成 + 窗口捕获 | **一片纯白**。Chromium 的画面由 GPU 进程画好直接交给系统合成器，窗口在 GDI 意义上是空的 |
| 不透明 + 软件渲染 + 窗口捕获 | ✅ **成功**。软件渲染把画面画进普通位图，采集才读得到 |
| 透明窗 + 截屏捕获 | 能采到动态画面，但**「截屏捕获」没有绿幕抠图**，背景永远去不掉 |

用户 2026-07-30 在软件里实地确认：**「窗口捕获」/「捕捉进程」/「捕捉游戏」有绿幕抠图，
「截屏捕获」没有**。这条是整件事的枢纽——中控台此前判断成"抠图只在摄像头上"，是错的。

**两个已经排除掉的方向，别再走**：
- 「允许窗口透明」（抖音官方文档的做法，`抖音玩法提审规则要点.md:54`）——用户早试过，无效。
- 模拟成摄像头（虚拟摄像头 + 摄像头源抠图）——技术上可行，但用户明确说暂不考虑，而且现在
  不需要了。

**已知质量风险**：立绘边缘是半透明抗锯齿像素，压在绿底上会混进绿色，抠完可能留一圈绿边。
先调抠图的相似度/羽化；调不掉就给角色加描边盖住边缘（美术决定，归用户）。
换底色：启动前 `set PET_CHROMA=#FF00FF`。默认绿是量过的——108 张立绘里离纯绿最远
（色距 196，品红 187、纯蓝 171）。

**为什么切换要重启**：`transparent` 是 BrowserWindow 的构造参数，硬件加速开关必须在
app ready 之前调用，**两者运行时都改不了**。所以控制台按钮做的是"带新参数重启一次"，
按钮上写明了。开播中（`PET_GRAB=1`）按钮会禁用——重启会走 envGuard 的 cleanup→arm，
等于在直播途中把根证书删了重装、系统代理拆了重设。

⚠️ **软件渲染要多吃 CPU**。260×340 的小窗理论上负担不大，但直播时本来就在跑游戏。真卡了
还有个更轻的中间档（只关 GPU 合成、保留 GPU 光栅化），未实现。

### 语音延迟与自问自答（2026-07-29 修，07-30 真人复测通过）

**结论先行**：从"讲 3~5 句等 2~3 分钟且自问自答停不下来"变成**停口到出声约 6.2 秒、不再自问自答**。
07-30 真人实测（BOYA 麦、第二句起的稳态）逐段：

```
VAD 静音判定 0.7s + 声纹 0.1s + 识别 2.0s + DeepSeek 1.4s + TTS 2.0s ≈ 6.2s
```

⚠️ **别拿"每句两三秒"当目标**——中控台曾这么承诺过，是说大了。上面五段里没有一段能靠工程手段
再砍一半：识别 2.0s 是 whisper-small 在 CPU 上的实际速度，TTS 2.0s 是到微软的一次网络往返，
VAD 0.7s 再压会把人说话说到一半就切断。真要大幅下降只能换本地 TTS + 更小的 ASR 模型，都要付
准确率或部署成本。

**首句仍然更慢**：DeepSeek 首次调用实测 6.8s（第二句 1.4s）、TTS 首次 4.2s（之后 2.0s），都是
建连接的一次性开销。TTS 那部分已用"开机就起常驻进程"消掉（07-30 改，**尚未在完整重启后复测**，
因为用户那次只重启了语音服务、没重启 Electron）。DeepSeek 的首次开销还没处理。


这条链跟抓包程序、直播伴侣完全没关系，正常启动魔丸就能复现，不用等开播。

用户 2026-07-29 实测一次后，读 `startup.log` 定位到**三件事**，前两件是真 bug：

#### ① 自问自答死循环（这才是"我只说两句她回一堆"的原因）——已修，已复测

日志实证：`14:14:43` 她说「在呢在呢~宝宝一直乖乖看着你直播呢！」→ `14:14:49` 麦克风听到
「在那在呢 宝宝一直乖乖 看着你直播呢」→ 又回一句 → 再被听见……连续 5 轮，最后是用户杀进程
才停的。**声纹没挡住合成音**（相似度过了 0.60，而声纹本来就是分"主播 vs 别人"、不是分
"人 vs 机器"）。

修法：桌宠出声期间直接丢掉麦克风帧。新增契约事件 `perception.audio.self_speaking`
（`packages/contract/events.md` 里有完整背景），character 发、perception-voice 订阅。
两侧各有超时兜底，防止信号丢了之后麦克风永久失聪。逻辑在 `run.py:SpeakGate`，
`verify_voice.py` 有 11 条用例钉死。

**07-30 复测通过**：日志里 `桌宠开始说话，暂停收音` → `桌宠说完了，恢复收音` 成对出现，
**其后没有任何"听到：<她刚说的话>"**。判断这条修好没有就看这一点，不是看有没有报错。

#### ② 首次说话干等 37 秒——已修，已复测

日志：第一段 `声纹12.4s 识别24.7s`，之后每段只要 `声纹0.0s 识别1.6~2.1s`。差的全是懒加载
（torch / faster-whisper 第一次用到才导入建模）。**这就是"2~3 分钟"里对不上的那一大段**——
而且它还会诱发二次伤害：等不到回应，用户会把同一句再说一遍，排队直接翻倍（日志里那两句
一模一样的「魔丸魔丸你在吗」就是这么来的，不是重复识别的 bug）。

修法：`run.py:_warmup()` 在开麦之前先拿一段噪声把两个模型都空跑一次，并且**预热完才**打印
"开始监听"，不让用户以为已经能听了。两个模型**并行**加载（互不相干：声纹走 torch、识别走
ctranslate2），07-30 实测 `声纹14.7s 识别33.6s 实际等待33.6s`——串行本来要等 48.3s。

📌 预热本身的耗时**跟系统文件缓存关系很大**，别把某一次的数字当基准：同一台机器实测过
声纹 12.4s / 28.7s / 14.7s 三种，冷盘那次最慢。所以"预热变慢了"不一定是代码退化。

#### ③ 每句固定 ~4 秒的合成开销——已压掉约 0.86 秒，剩下的是物理下限

⚠️ **这里推翻了一个此前写在本文档里的判断**：原先以为"把回复拆成短句、先说第一句"能把首字
等待从 5~7s 压到 2s。**实测证伪**——合成耗时跟文本长短几乎无关（4 字 3.06s / 36 字 2.78s，
短的反而更慢）。拆句只会让固定开销付好几遍，**更慢**。别再往这个方向改。

真实构成（2026-07-29 实测）：
- **~0.86s** python 解释器冷启动 + `import edge_tts`，每句白付一次 → **已消除**，改用常驻进程
  `services/tts/tts_daemon.py`（起不来/超时自动退回原来的命令行方式，宁可慢也不能说不出话）。
  07-30 复测：稳态 **1991ms**，对比旧的命令行方式 4131ms。
  ⚠️ **踩过一次**：常驻进程最初做成"第一次说话才启动"，于是它自己那 0.86s 启动照样压在第一句上
  ——实测第一句 4155ms，跟旧方式 4131ms 毫无差别，等于白做。已改成 `app.whenReady` 里就起
  （`main.js` 结尾处）。**任何"常驻/池化"的优化都要检查这一点：预热本身有没有被懒加载抵消掉。**
- **~2s** 到微软语音服务的一次网络往返，抖动 1.6~4.8s。**砍不掉**，除非换本地 TTS 引擎
  （见 CHANGELOG 待办 5a，成本高）。

另外把排队的下一句改成**在当前这句播放期间就去合成**（`renderer.js` 的 prefetch），多句连说
时后面几句的合成时间被播放盖住。brain 即时模板 + dialogue 追加回复正是这种两句连发的场景。

📌 **顺带测到、但没动**：本机 `HTTP(S)_PROXY` 指着 Shadowsocks，走代理合成 2.2~4.3s、不走
2.5~3.2s——代理平均慢约 0.5s 且长尾明显（出现过 12.6s）。**没有改成绕过代理**，因为直连能通
是当下的观测，网络条件一变就可能整个 TTS 都不通，这是环境权衡，留给用户定。

⚠️ 曾怀疑"关掉 SS 后 edge-tts 走死代理"，**实测已证伪**，别再往这个方向猜。

#### 还剩两件

1. **完整重启后复测第一句**：07-30 那次用户只重启了语音服务、没重启 Electron，所以"开机就起常驻
   TTS"这个改动还没被验证过。期望值：第一句的 `[tts][耗时]` 也在 2s 左右而不是 4.2s。
2. **DeepSeek 首次调用 6.8s**（第二句 1.4s）还没处理。跟 TTS 是同一类毛病——建连接的一次性开销，
   同样的解法（启动时预热连接）大概率管用，但要先确认这个开销是每次开播一次，还是更频繁。

#### 排查语音问题时先看这两行

```
[voice] 使用麦克风：…                 ← 不是 BOYA 就先停下，别往别处查
[voice][耗时] 语音段… 声纹… 识别… 队列积压…
```

⚠️ **07-30 真实踩到**：BOYA 没插好，程序静默退回主板 Realtek 声卡。声纹是用 BOYA 建的，换设备后
本人语音相似度会从 0.76~0.82 掉到 0.50~0.65，而放行阈值 0.60 就贴在中间——那次侥幸放行了，但
这正是"喊了没反应"的最常见成因，且现象跟程序故障无法区分。代码会打印实际设备名并在找不到
`PET_MIC` 时警告，**这行日志是排查的第一站**。

## 三、待办（按优先级）

**A. 真开播验证积压的那几批东西**：清单在第一节「待验证清单」（A/B/C 三组）。这是当前最优先的
事，其余待办都排在它后面——没验过真实效果之前，再往上叠新功能只会让问题更难定位。

**A′. 语音延迟与自问自答**（见第二节「语音延迟与自问自答」）：三处已定位并修复、离线全绿，
**只差真人复测**。跟 A 并列、不互相阻塞，不需要开播。

**A″. 记忆分层——设计已落地并提交（commit `cb755cd` `6fd7925`），效果尚未真开播验证**

session 模型：`观众↔桌宠` 一个 session（结构化，走 `viewers` 表），`主播↔桌宠` 另一个 session
（整场对话上下文 + 跨场档案），**软隔离**——两边共用同一个 SQLite，但只共享结构化事实
（昵称/档位/送礼额），不共享原始弹幕文本，也不共享主播档案。已实现：

1. 主播本场对话整场留在上下文（`Dialogue._streamer_turns`），不压缩不检索。
2. 下播总结"性格+相处方式"存进 `streamer_profile` 表（新模块 `services/dialogue/streamer_profile.py`），
   开播时读回来做成**冻结快照**拼进 system prompt，整场不再变（配合 DeepSeek 上下文缓存要求
   前缀完全一致）。只记性格，不记进度（玩到第几关下一场就过期）。
3. 观众有限记忆：`memory.evict_viewers()`，超过 10 场没来就忘（按场次不按天数，因为主播
   不定期开播）、最多记 30 人，member/star_guardian 手动打标的豁免淘汰。

验证脚本 `services/dialogue/verify_memory_layer.py`（24 项，纯离线不调真实模型），重点钉死
"回观众不带主播档案""模型没生成出内容时保留旧档案不写兜底台词进去"这两条。**没做过的**：
真实场景下"总结出来的性格像不像主播本人"没人工看过，提示词（`streamer_profile.build_summary_prompt`）
大概率要根据实际效果调。

**支撑数据（2026-07-30 实测，别再重算）**：
- DeepSeek V4 官方公告：`1M context is now the default across all official DeepSeek services`，
  flash / pro 都是 100 万 tokens。
- 中文密度**实测**（调 API 读 `usage.prompt_tokens`）：约 1.5~1.8 汉字/token；
  弹幕 24 tok/条、主播口播 25 tok/句、桌宠回复 20 tok/条。
- 3 小时一场估算：冷清 3.9 万 / 中等 10 万 / 爆火 45 万 tokens。**窗口不是瓶颈**。
  其中**主播侧整场只有约 1.7 万 tokens**，小到可以整场留着；量全在观众侧，所以观众侧不整体
  入上下文，走结构化。
- DeepSeek 上下文缓存**要求前缀完全一致**（官方 `guides/kv_cache`：`only hit the cache if it
  fully matches a cache prefix unit`）。→ 值得抄 Hermes 的**冻结快照**：长期记忆在开播时读进
  system prompt 后**整场不再变动**，中途更新写盘、下场生效，这样整场都能吃到前缀缓存。
- ⚠️ Hermes 的资料**只找到第三方博客，没有官方一手文档**，当"业界做法"参考，别当确切实现。

**决定不抄的**：FTS5 + 检索工具（多一次工具往返＝多一轮延迟，而我们整场装得下，没必要检索）；
80% 触发压缩（装得下就不用压）。我们的处境比 Hermes 简单，因为 session 有天然边界＝一场直播。

**B. 已确认要做、还没做**
1. **语音没有情感**：`emotion` 字段被 brain 算出来了，但在 `renderer.js → preload.js →
   main.js` 三层被丢弃，从没送进 TTS。edge-tts 本身支持语气参数，成本不高。
   📌 跟 A′ 的"流式合成/常驻进程"改的是同一段 TTS 代码，**一起做省一次返工**。
2. ✅ ~~dialogue 的「主播我的习惯」(streamer_notes)~~ 已用 `streamer_profile` 模块实现，
   见 A″ 一节，效果待开播验证。
3. ✅ ~~卡关攻略要知道在玩什么游戏 / 联网搜攻略~~ 07-30 下午已实现，见 CHANGELOG「卡关攻略
   接入真实联网搜索」一节。**留下的开放问题**：`GIFT_SPEAK_MIN_COINS=10` 这类常量目前是
   拍脑袋估的，等真实观众提问后可能要调查询词构造（`websearch.build_query`）的规则。
4. 实播彩排：竖屏/横屏区域校准（`command.calibrate` 目前只有发送端没有接收端，见 CHANGELOG
   「契约与集成」一节）+ 真实冒烟。
5. **两个产品判断，用户明确说"先别急"，但已经提出、需要在合适时机拍板**：
   - 闭嘴恢复后要不要延迟一会儿再触发主动互动（现在是恢复后冷却计时器不重置，可能立刻蹦一句
     "点关注"）。
   - 桌宠被吓到（`game.scare`）的时候要不要暂停主动互动，避免打断被吓的反应节奏。
6. 三份提案期文档（`提案文档草稿.md` / `提案Demo自查清单.md` / `抖音玩法提审规则要点.md`）
   已随方向变更作废，只剩参考价值，尚未标注。

**C. 已决定不做**
- 看插件·OCR：2026-07-29 用户决定弃用，代码保留在 `services/perception-ocr/` 不启动。
  **不要顺手删目录**，真要删是另一个决定。
- 防火墙加规则：用户决定直播时直接关防火墙，此事不再讨论。

## 四、坑（都是踩过的）

**环境**
- **以管理员身份启动第三方 exe 会被 auto mode 分类器拦死。** 用户明确说"继续"后重试**依然
  被拦**——这跟 `git merge` 那种"再确认一次就放行"不是一回事。不要换 shell 硬闯，如实告诉
  用户、改由他手动双击。
- 本机 Shadowsocks 常驻 `127.0.0.1:1080`，`HTTP(S)_PROXY` 环境变量指着它——**但这只是开发态**。
  ✅ **抓包程序的 `upstreamProxy` 不用配，保持空着**（2026-08-11 主播拍板）：**开播时 SS 是关着的**，
  没有"依赖 SS 的连接"需要保住。此前文档写的"必须配 `127.0.0.1:1080`，否则会切断排障通道"是错的
  ——那是把开发态当成了开播态。同理可参照卡关搜索为什么强制直连（见 CHANGELOG）。
  📌 写环境类约束时分清"谁在什么时候需要它"：**开播态和开发态是两套环境。**
- 抓包程序退出时**抹平**系统代理（ProxyEnable=0 + 清空），不是还原——会把 SS 设置一起干掉。
  `services/env-guard/` 已处理，且**还原前会先探测目标端口是否活着**（还原到死代理 = 整机
  断网，现象跟宽带故障无法区分）。
- 🔒 **红线：弹幕断流自动恢复流程里绝不允许强杀（`/F`）抓包程序。** 它正常退出时会把系统代理
  抹平、网络回到直连；强杀会跳过这一步，让系统代理仍然指着一个**没人监听的 8827 端口 = 整机
  断网**，现象跟宽带故障一模一样。宁可这次恢复失败也绝不强杀。`verify_danmaku.py` 有一条断言
  直接扫 `_restart_grabber` 的源码，谁以后图省事加个 `/F` 就会立刻红。
- `.cmd` 文件**必须 CRLF 换行**。LF 会让 cmd.exe 逐行解析错乱、连 REM 注释都漏出来当命令
  执行，报一堆 "is not recognized"，完全看不出真因。已加 `.gitattributes` 强制。
- 仓库路径含中文：`.cmd`/`.vbs` 的**内容**保持纯 ASCII（文件名可以中文），路径运行时从
  `%~dp0` 解析。从 Git Bash 调 `cmd //c` 会因代码页解析不出中文路径，用 PowerShell 调。
- 火绒可能拦抓包程序。**动杀毒软件前必须先问用户授权**，优先加信任规则而不是整个关闭。

**代码**
- Electron 主进程里**任何等外部 IO 的调用都不能用同步版本**（曾用 `spawnSync` 调 edge-tts，
  把主进程整个卡住，桌宠动作忽长忽短）。
- 夹具重放前**必须重启 brain**：`fixtures/danmaku/*.jsonl` 的 ts 是写死的，brain 的冷却用
  事件自带 ts，同一进程连播两遍第二遍会被静默吞掉。
- gitignore 掉的资源在 worktree 之间不共享（`assets/character/*.png` 那 108 张立绘、
  `voiceprint.npy`）。判断某个 gitignored 文件"缺失"前，先去主目录实际看一眼。
- DeepSeek `deepseek-v4-flash` 的 thinking 默认开启，不显式关会吃光 `max_tokens` 让正文变
  空字符串（`services/dialogue/chat.py` 已修）。

**教训**
- **环境漂移会让文档失真。** `requirements.txt` 列着 `rapidocr-onnxruntime` 但环境里没装，
  于是 6 处文档一直声称 OCR "✅ 真实可用"，实际 import 就报错。删掉 requirements 那一行才
  算断根——只改文字的话，下一个照着重建环境的人会把假象再造一遍。
- **验证/观察类组件绝不能同时充当数据链路。** 详见 memory 的
  `feedback_observer_not_in_datapath.md`。本项目的分工是：`run.py` 负责把弹幕送上总线（数据
  链路，永久），`record_grab.py` 只旁观判卷（阶段性，验完可关）。`verify_grab_end2end.py`
  里有一条测试专门把这个分工钉死。

## 五、验证脚本一览

改完任何东西，至少把相关的跑一遍（全部纯离线、不需要抓包程序）：

**⚠️ `verify_game.py` / `verify_walkthrough.py` 要用 `.venv` 的 python 跑（需要 numpy），
系统 python 会直接 ImportError。下面这份是 2026-08-11 实测的真实数字，全部真跑过、全绿：**

```
python services/perception-danmaku/verify_danmaku.py         # 41/41
python services/perception-danmaku/verify_grab_recorder.py   # 25/25
python services/perception-danmaku/verify_grab_end2end.py    # 16/16
python services/perception-game/verify_game.py               # 7/7    （需 .venv，见上）
python services/perception-game/verify_walkthrough.py        # 27/27  （需 .venv；卡关搜索链路）
python services/env-guard/verify_env_guard.py                # 31/31
python services/brain/verify_brain.py                        # 42/42
python services/dialogue/verify_dialogue.py                  # 107/107（真实调用视 key 而定）
python services/dialogue/verify_memory_layer.py              # 24/24  （记忆分层，纯离线不调真模型）
python services/perception-voice/verify_voice.py             # 34/34  （含掐麦用例）
python apps/control-panel/verify_control_panel.py            # 14/14
python services/tts/verify_tts.py                            # 6/6    ⚠️ 唯一要联网的
python scripts/verify_integration.py                         # 5/5
python scripts/verify_xruntime.py                            # PASS
node   apps/character/verify_character.js                    # 8/8
node   apps/character/test_renderer.js                       # 17 checks（含气泡队列/配速相关用例）
```

📌 **2026-08-11 顺带订正**：上一版这张表里有四个数字是过期或错的——`verify_danmaku` 31→**41**、
`verify_grab_recorder` 17→**25**、`verify_brain` 35→**42**（这三个是后续加了用例，表没跟上），
`verify_voice` 写的 36 其实**从来没有对过**（08-01 砍掉"免重复唤醒词窗口"时删掉了相关用例，
真实值是 34）。另外 `verify_control_panel.py` 本来漏在表外。**这张表以后每次全量跑完就照抄一遍，
不要手改数字**——它的用处正是"数字对不上就说明有东西变了"。

`verify_tts.py` 是唯一**需要联网**的（edge-tts 是微软的云服务）。没装依赖或网络不通时，
真实合成那两项会自动 SKIP 而不是 FAIL——不能让一个联网检查把离线全绿搞成红的。

`services/perception-game/verify_walkthrough.py` 是**纯离线**的（探测函数、搜索、模型调用
全部注入假实现），重点测的不是"能不能搜到"，而是**搜不到时会不会编**——这条链路存在的
全部理由。真实的联网搜索（复用桌宠 Electron 开隐藏窗口）没有专门的自动化验证脚本，是
07-30 当天手动跑真实调用验证的，见 CHANGELOG。

## 六、明确不做的事

不在 worktree 占用的文件里写 feature 代码；不评判美术/声音质量；不替用户做产品定位判断；
不做不可逆的对外操作；不确定的事实不猜，去查或去问。
